diff --git a/dist/chunk-2OAKX4SZ.js b/dist/chunk-2OAKX4SZ.js
index ebefca352b131b8e86021a6cc6001558adbdfebf..58782f233347fa7a16827aa18c231b9a9a5a3d5a 100644
--- a/dist/chunk-2OAKX4SZ.js
+++ b/dist/chunk-2OAKX4SZ.js
@@ -18,11 +18,11 @@ __export(vercel_exports, {
   getSystemPrompt: () => getSystemPrompt
 });
 import { tool } from "ai";
-import { z } from "zod";
+import * as z from "zod";
 var editFileSchema = z.object({
-  target_filepath: z.string().describe("The path of the target file to modify"),
-  instruction: z.string().describe("A single sentence written in the first person describing what the agent is changing. Used to help disambiguate uncertainty in the edit."),
-  code_edit: z.string().describe("Specify ONLY the precise lines of code that you wish to edit. Use `// ... existing code ...` for unchanged sections.")
+  target_filepath: z.string().trim().meta({ description: "The path of the target file to modify" }),
+  instruction: z.string().trim().meta({ description: "A single sentence written in the first person describing what the agent is changing. Used to help disambiguate uncertainty in the edit." }),
+  code_edit: z.string().trim().meta({ description: "Specify ONLY the precise lines of code that you wish to edit. Use `// ... existing code ...` for unchanged sections." })
 });
 var editFileTool = tool({
   description: EDIT_FILE_TOOL_DESCRIPTION,
@@ -49,9 +49,9 @@ function getSystemPrompt() {
 }
 function createEditFileTool(config = {}) {
   const schema = z.object({
-    target_filepath: z.string().describe("The path of the target file to modify"),
-    instruction: z.string().describe("A single sentence written in the first person describing what the agent is changing. Used to help disambiguate uncertainty in the edit."),
-    code_edit: z.string().describe("Specify ONLY the precise lines of code that you wish to edit. Use `// ... existing code ...` for unchanged sections.")
+    target_filepath: z.string().trim().meta({ description: "The path of the target file to modify" }),
+    instruction: z.string().trim().meta({ description: "A single sentence written in the first person describing what the agent is changing. Used to help disambiguate uncertainty in the edit." }),
+    code_edit: z.string().trim().meta({ description: "Specify ONLY the precise lines of code that you wish to edit. Use `// ... existing code ...` for unchanged sections." })
   });
   return tool({
     description: config.description || EDIT_FILE_TOOL_DESCRIPTION,
diff --git a/dist/chunk-3BCKZKNK.js b/dist/chunk-3BCKZKNK.js
index 92ee23b1fead27e732733d08045761d0fae9dddf..2dd226fe0401d6103ea3cb576775a02097470ddb 100644
--- a/dist/chunk-3BCKZKNK.js
+++ b/dist/chunk-3BCKZKNK.js
@@ -42,8 +42,7 @@ function countChanges(original, modified) {
 async function callMorphAPI(originalCode, codeEdit, instructions, filepath, config) {
   const apiKey = config.morphApiKey || (typeof process !== "undefined" ? process.env?.MORPH_API_KEY : void 0);
   const apiUrl = config.morphApiUrl || DEFAULT_API_URL;
-  const useLarge = config.large ?? (typeof process !== "undefined" ? process.env?.MORPH_LARGE_APPLY !== "false" : true);
-  const model = useLarge ? "morph-v3-large" : "morph-v3-fast";
+  const model = config.model ?? (config.large !== void 0 ? config.large ? "morph-v3-large" : "morph-v3-fast" : typeof process !== "undefined" ? process.env?.MORPH_APPLY_MODEL ?? (process.env?.MORPH_LARGE_APPLY !== void 0 ? process.env.MORPH_LARGE_APPLY !== "false" ? "morph-v3-large" : "morph-v3-fast" : "auto") : "auto");
   const timeout = config.timeout || DEFAULT_TIMEOUT;
   const debug = config.debug || false;
   if (!apiKey) {
diff --git a/dist/chunk-3XRNC56L.js b/dist/chunk-3XRNC56L.js
index 523dff1a63d95776ca368836195453c183272367..e4b86eac32bdfcf460cbd9831817b30381f9c1e4 100644
--- a/dist/chunk-3XRNC56L.js
+++ b/dist/chunk-3XRNC56L.js
@@ -7,13 +7,13 @@ import {
 
 // tools/codebase_search/vercel.ts
 import { tool } from "ai";
-import { z } from "zod";
+import * as z from "zod";
 function createCodebaseSearchTool(config) {
   const schema = z.object({
-    query: z.string().describe('A complete question about what you want to understand. Ask as if talking to a colleague: "How does X work?", "What happens when Y?", "Where is Z handled?"'),
-    target_directories: z.array(z.string()).describe("Prefix directory paths to limit search scope (single directory only, no glob patterns). Use [] to search entire repo."),
-    explanation: z.string().describe("One sentence explanation as to why this tool is being used, and how it contributes to the goal."),
-    limit: z.number().optional().describe("Max results to return (default: 10)")
+    query: z.string().trim().meta({ description: 'A complete question about what you want to understand. Ask as if talking to a colleague: "How does X work?", "What happens when Y?", "Where is Z handled?"' }),
+    target_directories: z.array(z.string().trim()).meta({ description: "Prefix directory paths to limit search scope (single directory only, no glob patterns). Use [] to search entire repo." }),
+    explanation: z.string().trim().meta({ description: "One sentence explanation as to why this tool is being used, and how it contributes to the goal." }),
+    limit: z.number().optional().meta({ description: "Max results to return (default: 10)" })
   });
   return tool({
     description: CODEBASE_SEARCH_DESCRIPTION,
diff --git a/dist/chunk-63VHBANJ.js b/dist/chunk-63VHBANJ.js
index c16fb033c201fda8afde8982e01bfa37766d9a5e..b313dce75e1cf317b12e27f9d22eeda9552005d0 100644
--- a/dist/chunk-63VHBANJ.js
+++ b/dist/chunk-63VHBANJ.js
@@ -1,12 +1,12 @@
 // tools/warp_grep/utils/github.ts
 function parseGitHubUrl(input) {
   input = input.trim();
-  const urlPattern = /^(?:https?:\/\/)?github\.com\/([^\/]+)\/([^\/\?\#]+)/;
+  const urlPattern = /^(?:https?:\/\/)?github\.com\/([^/]+)\/([^/?#]+)/;
   const match = input.match(urlPattern);
   if (match) {
     return { owner: match[1], repo: match[2].replace(/\.git$/, "") };
   }
-  const shortPattern = /^([^\/]+)\/([^\/]+)$/;
+  const shortPattern = /^([^/]+)\/([^/]+)$/;
   const shortMatch = input.match(shortPattern);
   if (shortMatch && !shortMatch[1].includes(".")) {
     return { owner: shortMatch[1], repo: shortMatch[2].replace(/\.git$/, "") };
diff --git a/dist/chunk-C37YAYBK.js b/dist/chunk-C37YAYBK.js
index ea2b3e526a55f7de8808b6e90cfad316388bbd4e..f8a70833528f8a5b1a218027ba783610e39a235b 100644
--- a/dist/chunk-C37YAYBK.js
+++ b/dist/chunk-C37YAYBK.js
@@ -150,6 +150,9 @@ async function callModel(messages, model, options = {}) {
     defaultHeaders: { "X-Morph-SDK-Version": SDK_VERSION }
   });
   const MAX_EMPTY_RETRIES = 1;
+  const temperature = options.temperature ?? 0;
+  const maxTokens = options.maxTokens ?? 2048;
+  const useBuiltinTools = options.useBuiltinTools === true;
   logger.debug("WarpGrep", "model_call", { url: baseURL, model, message_count: messages.length });
   const callStartTime = Date.now();
   for (let attempt = 0; attempt <= MAX_EMPTY_RETRIES; attempt++) {
@@ -157,10 +160,10 @@ async function callModel(messages, model, options = {}) {
     try {
       data = await client.chat.completions.create({
         model,
-        temperature: 0,
-        max_tokens: 2048,
+        temperature,
+        max_tokens: maxTokens,
         messages,
-        tools: TOOL_SPECS,
+        ...useBuiltinTools ? {} : { tools: TOOL_SPECS },
         ...options.search_type ? { search_type: options.search_type } : {}
       });
     } catch (error) {
@@ -261,8 +264,11 @@ async function* runWarpGrepStreaming(config) {
   const timings = { turns: [], timeout_ms: timeoutMs };
   const repoRoot = config.repoRoot || process.cwd();
   const model = config.model || DEFAULT_MODEL;
+  const temperature = config.temperature;
+  const maxTokens = config.maxTokens;
+  const useBuiltinTools = config.useBuiltinTools === true;
   const messages = [];
-  const maxTurns = AGENT_CONFIG.MAX_TURNS;
+  const maxTurns = config.maxTurns ?? AGENT_CONFIG.MAX_TURNS;
   logger.debug("WarpGrep", "session_start", { search_term: config.searchTerm, model, max_turns: maxTurns });
   const initialStateStart = Date.now();
   const initialState = await buildInitialState(repoRoot, config.searchTerm, config.provider, { search_type: config.search_type });
@@ -288,7 +294,10 @@ async function* runWarpGrepStreaming(config) {
       morphApiUrl: config.morphApiUrl,
       retryConfig: config.retryConfig,
       timeout: timeoutMs,
-      search_type: config.search_type
+      search_type: config.search_type,
+      temperature,
+      maxTokens,
+      useBuiltinTools
     }).catch((e) => {
       const errMsg = e instanceof Error ? e.message : String(e);
       console.error(`[warp_grep] Morph API call failed on turn ${turn}:`, errMsg);
diff --git a/dist/chunk-HYRHI2UL.js b/dist/chunk-HYRHI2UL.js
index 20183108bf64aa3d27c751b14e1de0ce5809a075..b4f38a74427f3fb8a313b986a8975b6603b5051c 100644
--- a/dist/chunk-HYRHI2UL.js
+++ b/dist/chunk-HYRHI2UL.js
@@ -1,7 +1,7 @@
 // tools/warp_grep/index.ts
-import { z } from "zod";
+import * as z from "zod";
 var warpGrepInputSchema = z.object({
-  search_term: z.string().describe("Search problem statement that this subagent is supposed to research for")
+  search_term: z.string().trim().meta({ description: "Search problem statement that this subagent is supposed to research for" })
 });
 
 export {
diff --git a/dist/chunk-IGQYZ2KH.js b/dist/chunk-IGQYZ2KH.js
index 969ad2fcca027696c3243fe51c66f9cc7699bf9b..04d38359e5e4e2dac3e2aef8e2f73739bbfe36eb 100644
--- a/dist/chunk-IGQYZ2KH.js
+++ b/dist/chunk-IGQYZ2KH.js
@@ -10,7 +10,7 @@ import {
 
 // subagents/vercel.ts
 import { tool, generateText, stepCountIs } from "ai";
-import { z } from "zod";
+import * as z from "zod";
 var DEFAULT_MAX_TURNS = {
   quick: 3,
   medium: 6,
@@ -39,10 +39,10 @@ function createExploreSubagent(config) {
     const runPromise = (async () => {
       try {
         const codebaseSearchSchema = z.object({
-          searchRequest: z.string().describe("Natural language description of what to search for")
+          searchRequest: z.string().trim().meta({ description: "Natural language description of what to search for" })
         });
         const sendMessageSchema = z.object({
-          message: z.string().describe("The message to send to the orchestrator")
+          message: z.string().trim().meta({ description: "The message to send to the orchestrator" })
         });
         const result = await generateText({
           model: config.model,
@@ -147,7 +147,7 @@ function createExploreSubagent(config) {
     return runPromise;
   }
   const exploreToolSchema = z.object({
-    searchRequest: z.string().describe("What to explore in the codebase. Be specific about what you want to understand.")
+    searchRequest: z.string().trim().meta({ description: "What to explore in the codebase. Be specific about what you want to understand." })
   });
   const exploreTool = tool({
     description: "Explore a codebase to answer questions about code structure, find implementations, trace call chains, and understand architecture. Returns a summary of findings. Use this for broad exploration rather than pinpoint keyword searches.",
diff --git a/dist/chunk-JLLIVBKY.js b/dist/chunk-JLLIVBKY.js
index d39b5ce08f4086d20829ef56cd2b322639177ab5..503f1673747d2873828a7d4b3e5acdac08cdcd05 100644
--- a/dist/chunk-JLLIVBKY.js
+++ b/dist/chunk-JLLIVBKY.js
@@ -20,9 +20,14 @@ import {
 
 // tools/warp_grep/client.ts
 var DEFAULT_CODE_SEARCH_URL = "https://morphllm.com";
-async function getLocalProvider(repoRoot, excludes, allowNames) {
+async function getLocalProvider(repoRoot, excludes, allowNames, includes) {
   const { LocalRipgrepProvider } = await import("./tools/warp_grep/providers/local.js");
-  const opts = allowNames?.length ? { allowNames } : void 0;
+  const hasAllowNames = allowNames?.length;
+  const hasIncludes = includes?.length;
+  const opts = hasAllowNames || hasIncludes ? {
+    ...hasAllowNames ? { allowNames } : {},
+    ...hasIncludes ? { includes } : {}
+  } : void 0;
   return new LocalRipgrepProvider(repoRoot, excludes, opts);
 }
 var WarpGrepClient = class {
@@ -42,7 +47,12 @@ var WarpGrepClient = class {
       morphApiUrl: this.config.morphApiUrl,
       retryConfig: this.config.retryConfig,
       timeout: this.config.timeout,
-      search_type: input.search_type
+      search_type: input.search_type,
+      model: input.model ?? this.config.model,
+      temperature: input.temperature ?? this.config.temperature,
+      maxTokens: input.maxTokens ?? this.config.maxTokens,
+      maxTurns: input.maxTurns ?? this.config.maxTurns,
+      useBuiltinTools: input.useBuiltinTools ?? this.config.useBuiltinTools
     };
     if (input.streamSteps) {
       return executeToolCallStreaming({ search_term: input.searchTerm }, toolConfig);
@@ -137,7 +147,7 @@ function executeWarpGrep(input, config) {
 async function executeToolCall(input, config) {
   const parsed = typeof input === "string" ? JSON.parse(input) : input;
   const allowNames = config.search_type === "node_modules" ? ["node_modules"] : void 0;
-  const provider = config.remoteCommands ? new RemoteCommandsProvider(config.repoRoot, config.remoteCommands) : config.provider ?? await getLocalProvider(config.repoRoot, config.excludes, allowNames);
+  const provider = config.remoteCommands ? new RemoteCommandsProvider(config.repoRoot, config.remoteCommands) : config.provider ?? await getLocalProvider(config.repoRoot, config.excludes, allowNames, config.includes);
   const result = await runWarpGrep({
     searchTerm: parsed.search_term,
     repoRoot: config.repoRoot,
@@ -149,16 +159,22 @@ async function executeToolCall(input, config) {
     morphApiUrl: config.morphApiUrl,
     retryConfig: config.retryConfig,
     timeout: config.timeout,
-    search_type: config.search_type
+    search_type: config.search_type,
+    model: config.model,
+    temperature: config.temperature,
+    maxTokens: config.maxTokens,
+    maxTurns: config.maxTurns,
+    useBuiltinTools: config.useBuiltinTools
   });
   return processAgentResult(result, parsed.search_term, config.morphApiKey, config.morphApiUrl);
 }
 async function processAgentResult(result, searchTerm, apiKey, apiUrl) {
+  const timings = result.timings;
   const finish = result.finish;
   if (result.terminationReason !== "completed" || !finish?.metadata) {
     const errorDetails = result.errors?.map((e) => e.message).join("; ") || "unknown reason";
     console.error(`[warp_grep] processAgentResult failed. Reason: ${result.terminationReason}. Errors: ${errorDetails}. Turns: ${result.timings?.turns?.length ?? 0}`);
-    return { success: false, error: `Search did not complete: ${errorDetails}` };
+    return { success: false, error: `Search did not complete: ${errorDetails}`, ...timings ? { timings } : {} };
   }
   const contexts = (finish.resolved ?? []).map((r) => ({
     file: r.path,
@@ -174,12 +190,12 @@ async function processAgentResult(result, searchTerm, apiKey, apiUrl) {
   } catch (error) {
     console.error(`[warp_grep] Failed to compact result contexts:`, error);
   }
-  return { success: true, contexts, summary: finish.payload };
+  return { success: true, contexts, summary: finish.payload, ...timings ? { timings } : {} };
 }
 async function* executeToolCallStreaming(input, config) {
   const parsed = typeof input === "string" ? JSON.parse(input) : input;
   const allowNames = config.search_type === "node_modules" ? ["node_modules"] : void 0;
-  const provider = config.remoteCommands ? new RemoteCommandsProvider(config.repoRoot, config.remoteCommands) : config.provider ?? await getLocalProvider(config.repoRoot, config.excludes, allowNames);
+  const provider = config.remoteCommands ? new RemoteCommandsProvider(config.repoRoot, config.remoteCommands) : config.provider ?? await getLocalProvider(config.repoRoot, config.excludes, allowNames, config.includes);
   const generator = runWarpGrepStreaming({
     searchTerm: parsed.search_term,
     repoRoot: config.repoRoot,
@@ -191,7 +207,12 @@ async function* executeToolCallStreaming(input, config) {
     morphApiUrl: config.morphApiUrl,
     retryConfig: config.retryConfig,
     timeout: config.timeout,
-    search_type: config.search_type
+    search_type: config.search_type,
+    model: config.model,
+    temperature: config.temperature,
+    maxTokens: config.maxTokens,
+    maxTurns: config.maxTurns,
+    useBuiltinTools: config.useBuiltinTools
   });
   let agentResult;
   for (; ; ) {
diff --git a/dist/chunk-LE66XCOI.js b/dist/chunk-LE66XCOI.js
index 50255ad09c9022e95e3b2270049e4237fa85ab9b..78e74253adc1111c285ae7734aadda9df6d15d4d 100644
--- a/dist/chunk-LE66XCOI.js
+++ b/dist/chunk-LE66XCOI.js
@@ -118,7 +118,7 @@ var MorphGit = class {
     const remotes = await git.listRemotes({ fs, dir });
     const originRemote = remotes.find((r) => r.remote === remote);
     if (originRemote) {
-      const match = originRemote.url.match(/\/repos\/([^\/]+)$/);
+      const match = originRemote.url.match(/\/repos\/([^/]+)$/);
       if (match) {
         repoId = match[1];
       }
diff --git a/dist/chunk-OPNTDMHH.js b/dist/chunk-OPNTDMHH.js
index 225ef411981cfdd8027b67710c2d5bab49915752..cf00b3ce23aaab8c3e14ef8f242b91506df212a4 100644
--- a/dist/chunk-OPNTDMHH.js
+++ b/dist/chunk-OPNTDMHH.js
@@ -111,8 +111,12 @@ var LocalRipgrepProvider = class {
     if (options?.allowNames?.length) {
       this.allowNames = new Set(options.allowNames);
     }
+    if (options?.includes?.length) {
+      this.includes = [...options.includes];
+    }
   }
   allowNames;
+  includes;
   async grep(params) {
     let abs;
     try {
@@ -139,6 +143,7 @@ var LocalRipgrepProvider = class {
       contextLines,
       ...params.case_sensitive === false ? ["--ignore-case"] : [],
       ...params.glob ? ["--glob", params.glob] : [],
+      ...this.includes ? this.includes.flatMap((g) => ["-g", g]) : [],
       ...this.excludes.filter((e) => !this.allowNames?.has(e)).flatMap((e) => ["-g", `!${e}`]),
       params.pattern,
       targetArg || "."
@@ -314,6 +319,7 @@ Details: ${res.stderr}` : ""}`
       "--color=never",
       "-g",
       params.pattern,
+      ...this.includes ? this.includes.flatMap((g) => ["-g", g]) : [],
       ...this.excludes.filter((e) => !this.allowNames?.has(e)).flatMap((e) => ["-g", `!${e}`]),
       targetArg || "."
     ];
diff --git a/dist/chunk-TAS6S42A.js b/dist/chunk-TAS6S42A.js
index 1fcc0d16793f3cf8b84e6bc6e1a138705c1fa944..f4c116529a69529a2b8a159007e5014eb44adad6 100644
--- a/dist/chunk-TAS6S42A.js
+++ b/dist/chunk-TAS6S42A.js
@@ -12,7 +12,7 @@ import {
 
 // tools/warp_grep/vercel.ts
 import { tool } from "ai";
-import { z } from "zod";
+import * as z from "zod";
 var warpGrepJsonSchema = {
   type: "object",
   properties: {
@@ -29,7 +29,7 @@ async function execute(input, config) {
 }
 function createWarpGrepTool(config) {
   const schema = config.inputSchema ?? z.object({
-    search_term: z.string().describe("Search problem statement that this subagent is supposed to research for")
+    search_term: z.string().trim().meta({ description: "Search problem statement that this subagent is supposed to research for" })
   });
   return tool({
     description: config.description ?? WARP_GREP_DESCRIPTION,
@@ -64,10 +64,10 @@ function createGitHubSearchTool(config) {
   return tool({
     description: GITHUB_SEARCH_DESCRIPTION,
     inputSchema: z.object({
-      search_term: z.string().describe("Search problem statement that this subagent is supposed to research for"),
-      github_url: z.string().optional().describe('GitHub repository URL to search (e.g. "https://github.com/vercel/next.js"). You must provide either github_url or owner_repo.'),
-      owner_repo: z.string().optional().describe('Repository owner/repo shorthand (e.g. "vercel/next.js"). You must provide either github_url or owner_repo.'),
-      branch: z.string().optional().describe("Branch to search (defaults to repo default branch)")
+      search_term: z.string().trim().meta({ description: "Search problem statement that this subagent is supposed to research for" }),
+      github_url: z.string().trim().optional().meta({ description: 'GitHub repository URL to search (e.g. "https://github.com/vercel/next.js"). You must provide either github_url or owner_repo.' }),
+      owner_repo: z.string().trim().optional().meta({ description: 'Repository owner/repo shorthand (e.g. "vercel/next.js"). You must provide either github_url or owner_repo.' }),
+      branch: z.string().trim().optional().meta({ description: "Branch to search (defaults to repo default branch)" })
     }),
     execute: async (params) => {
       const github = params.github_url || params.owner_repo;
@@ -90,11 +90,11 @@ function createGitHubReadFileTool(config) {
   return tool({
     description: GITHUB_READ_FILE_DESCRIPTION,
     inputSchema: z.object({
-      github: z.string().describe('owner/repo shorthand (e.g., "vercel/next.js")'),
-      path: z.string().describe("File path within the repository"),
-      startLine: z.number().optional().describe("Start line number (1-based)"),
-      endLine: z.number().optional().describe("End line number (1-based, inclusive)"),
-      branch: z.string().optional().describe("Branch to read from (defaults to repo default branch)")
+      github: z.string().trim().meta({ description: 'owner/repo shorthand (e.g., "vercel/next.js")' }),
+      path: z.string().trim().meta({ description: "File path within the repository" }),
+      startLine: z.number().optional().meta({ description: "Start line number (1-based)" }),
+      endLine: z.number().optional().meta({ description: "End line number (1-based, inclusive)" }),
+      branch: z.string().trim().optional().meta({ description: "Branch to read from (defaults to repo default branch)" })
     }),
     execute: async (params) => {
       const result = await executeGitHubReadFile(params, config);
diff --git a/dist/chunk-Y6T4NA75.js b/dist/chunk-Y6T4NA75.js
index df2e7855aaec4c2a606eebadce817292fe24ec2f..3d21e2e889600ad7e576e3b1f9254204b3d23357 100644
--- a/dist/chunk-Y6T4NA75.js
+++ b/dist/chunk-Y6T4NA75.js
@@ -15,13 +15,13 @@ __export(vercel_exports, {
   createBrowserTool: () => createBrowserTool
 });
 import { tool as createTool } from "ai";
-import { z } from "zod";
+import * as z from "zod";
 function createBrowserTool(config) {
   const schema = z.object({
-    task: z.string().describe('Natural language description of what to do (e.g., "Test checkout flow for buying a pineapple")'),
-    url: z.string().optional().describe("Starting URL (e.g., https://3000-xyz.e2b.dev)"),
-    maxSteps: z.number().min(1).max(50).default(10).describe("Maximum number of browser actions to take"),
-    region: z.enum(["sfo", "lon"]).default("sfo").describe("Browserless region: sfo (US West) or lon (Europe)")
+    task: z.string().trim().meta({ description: 'Natural language description of what to do (e.g., "Test checkout flow for buying a pineapple")' }),
+    url: z.string().trim().optional().meta({ description: "Starting URL (e.g., https://3000-xyz.e2b.dev)" }),
+    maxSteps: z.number().min(1).max(50).default(10).meta({ description: "Maximum number of browser actions to take" }),
+    region: z.enum(["sfo", "lon"]).default("sfo").meta({ description: "Browserless region: sfo (US West) or lon (Europe)" })
   });
   return createTool({
     description: BROWSER_TOOL_DESCRIPTION,
diff --git a/dist/client.cjs b/dist/client.cjs
index 5ddaf3d836b16ad5a95b3b53e5bcd32670e366f8..e161c3114c7deb75c5e072fcf8e46d0523a480ac 100644
--- a/dist/client.cjs
+++ b/dist/client.cjs
@@ -382,8 +382,7 @@ function countChanges(original, modified) {
 async function callMorphAPI(originalCode, codeEdit, instructions, filepath, config) {
   const apiKey = config.morphApiKey || (typeof process !== "undefined" ? process.env?.MORPH_API_KEY : void 0);
   const apiUrl = config.morphApiUrl || DEFAULT_API_URL;
-  const useLarge = config.large ?? (typeof process !== "undefined" ? process.env?.MORPH_LARGE_APPLY !== "false" : true);
-  const model = useLarge ? "morph-v3-large" : "morph-v3-fast";
+  const model = config.model ?? (config.large !== void 0 ? config.large ? "morph-v3-large" : "morph-v3-fast" : typeof process !== "undefined" ? process.env?.MORPH_APPLY_MODEL ?? (process.env?.MORPH_LARGE_APPLY !== void 0 ? process.env.MORPH_LARGE_APPLY !== "false" ? "morph-v3-large" : "morph-v3-fast" : "auto") : "auto");
   const timeout = config.timeout || DEFAULT_TIMEOUT;
   const debug = config.debug || false;
   if (!apiKey) {
@@ -849,8 +848,12 @@ var init_local = __esm({
         if (options?.allowNames?.length) {
           this.allowNames = new Set(options.allowNames);
         }
+        if (options?.includes?.length) {
+          this.includes = [...options.includes];
+        }
       }
       allowNames;
+      includes;
       async grep(params) {
         let abs;
         try {
@@ -877,6 +880,7 @@ var init_local = __esm({
           contextLines,
           ...params.case_sensitive === false ? ["--ignore-case"] : [],
           ...params.glob ? ["--glob", params.glob] : [],
+          ...this.includes ? this.includes.flatMap((g) => ["-g", g]) : [],
           ...this.excludes.filter((e) => !this.allowNames?.has(e)).flatMap((e) => ["-g", `!${e}`]),
           params.pattern,
           targetArg || "."
@@ -1052,6 +1056,7 @@ Details: ${res.stderr}` : ""}`
           "--color=never",
           "-g",
           params.pattern,
+          ...this.includes ? this.includes.flatMap((g) => ["-g", g]) : [],
           ...this.excludes.filter((e) => !this.allowNames?.has(e)).flatMap((e) => ["-g", `!${e}`]),
           targetArg || "."
         ];
@@ -3043,6 +3048,9 @@ async function callModel(messages, model, options = {}) {
     defaultHeaders: { "X-Morph-SDK-Version": SDK_VERSION }
   });
   const MAX_EMPTY_RETRIES = 1;
+  const temperature = options.temperature ?? 0;
+  const maxTokens = options.maxTokens ?? 2048;
+  const useBuiltinTools = options.useBuiltinTools === true;
   logger.debug("WarpGrep", "model_call", { url: baseURL, model, message_count: messages.length });
   const callStartTime = Date.now();
   for (let attempt = 0; attempt <= MAX_EMPTY_RETRIES; attempt++) {
@@ -3050,10 +3058,10 @@ async function callModel(messages, model, options = {}) {
     try {
       data = await client.chat.completions.create({
         model,
-        temperature: 0,
-        max_tokens: 2048,
+        temperature,
+        max_tokens: maxTokens,
         messages,
-        tools: TOOL_SPECS,
+        ...useBuiltinTools ? {} : { tools: TOOL_SPECS },
         ...options.search_type ? { search_type: options.search_type } : {}
       });
     } catch (error) {
@@ -3154,8 +3162,11 @@ async function* runWarpGrepStreaming(config) {
   const timings = { turns: [], timeout_ms: timeoutMs };
   const repoRoot = config.repoRoot || process.cwd();
   const model = config.model || DEFAULT_MODEL;
+  const temperature = config.temperature;
+  const maxTokens = config.maxTokens;
+  const useBuiltinTools = config.useBuiltinTools === true;
   const messages = [];
-  const maxTurns = AGENT_CONFIG.MAX_TURNS;
+  const maxTurns = config.maxTurns ?? AGENT_CONFIG.MAX_TURNS;
   logger.debug("WarpGrep", "session_start", { search_term: config.searchTerm, model, max_turns: maxTurns });
   const initialStateStart = Date.now();
   const initialState = await buildInitialState(repoRoot, config.searchTerm, config.provider, { search_type: config.search_type });
@@ -3181,7 +3192,10 @@ async function* runWarpGrepStreaming(config) {
       morphApiUrl: config.morphApiUrl,
       retryConfig: config.retryConfig,
       timeout: timeoutMs,
-      search_type: config.search_type
+      search_type: config.search_type,
+      temperature,
+      maxTokens,
+      useBuiltinTools
     }).catch((e) => {
       const errMsg = e instanceof Error ? e.message : String(e);
       console.error(`[warp_grep] Morph API call failed on turn ${turn}:`, errMsg);
@@ -3559,12 +3573,12 @@ function createCodeStorageHttpCommands(config) {
 // tools/warp_grep/utils/github.ts
 function parseGitHubUrl(input) {
   input = input.trim();
-  const urlPattern = /^(?:https?:\/\/)?github\.com\/([^\/]+)\/([^\/\?\#]+)/;
+  const urlPattern = /^(?:https?:\/\/)?github\.com\/([^/]+)\/([^/?#]+)/;
   const match = input.match(urlPattern);
   if (match) {
     return { owner: match[1], repo: match[2].replace(/\.git$/, "") };
   }
-  const shortPattern = /^([^\/]+)\/([^\/]+)$/;
+  const shortPattern = /^([^/]+)\/([^/]+)$/;
   const shortMatch = input.match(shortPattern);
   if (shortMatch && !shortMatch[1].includes(".")) {
     return { owner: shortMatch[1], repo: shortMatch[2].replace(/\.git$/, "") };
@@ -3701,9 +3715,14 @@ async function compactFileContexts(files, query, apiKey, apiUrl, maxTokens = 1e4
 
 // tools/warp_grep/client.ts
 var DEFAULT_CODE_SEARCH_URL = "https://morphllm.com";
-async function getLocalProvider(repoRoot, excludes, allowNames) {
+async function getLocalProvider(repoRoot, excludes, allowNames, includes) {
   const { LocalRipgrepProvider: LocalRipgrepProvider2 } = await Promise.resolve().then(() => (init_local(), local_exports));
-  const opts = allowNames?.length ? { allowNames } : void 0;
+  const hasAllowNames = allowNames?.length;
+  const hasIncludes = includes?.length;
+  const opts = hasAllowNames || hasIncludes ? {
+    ...hasAllowNames ? { allowNames } : {},
+    ...hasIncludes ? { includes } : {}
+  } : void 0;
   return new LocalRipgrepProvider2(repoRoot, excludes, opts);
 }
 var WarpGrepClient = class {
@@ -3723,7 +3742,12 @@ var WarpGrepClient = class {
       morphApiUrl: this.config.morphApiUrl,
       retryConfig: this.config.retryConfig,
       timeout: this.config.timeout,
-      search_type: input.search_type
+      search_type: input.search_type,
+      model: input.model ?? this.config.model,
+      temperature: input.temperature ?? this.config.temperature,
+      maxTokens: input.maxTokens ?? this.config.maxTokens,
+      maxTurns: input.maxTurns ?? this.config.maxTurns,
+      useBuiltinTools: input.useBuiltinTools ?? this.config.useBuiltinTools
     };
     if (input.streamSteps) {
       return executeToolCallStreaming({ search_term: input.searchTerm }, toolConfig);
@@ -3811,7 +3835,7 @@ var WarpGrepClient = class {
 async function executeToolCall(input, config) {
   const parsed = typeof input === "string" ? JSON.parse(input) : input;
   const allowNames = config.search_type === "node_modules" ? ["node_modules"] : void 0;
-  const provider = config.remoteCommands ? new RemoteCommandsProvider(config.repoRoot, config.remoteCommands) : config.provider ?? await getLocalProvider(config.repoRoot, config.excludes, allowNames);
+  const provider = config.remoteCommands ? new RemoteCommandsProvider(config.repoRoot, config.remoteCommands) : config.provider ?? await getLocalProvider(config.repoRoot, config.excludes, allowNames, config.includes);
   const result = await runWarpGrep({
     searchTerm: parsed.search_term,
     repoRoot: config.repoRoot,
@@ -3823,16 +3847,22 @@ async function executeToolCall(input, config) {
     morphApiUrl: config.morphApiUrl,
     retryConfig: config.retryConfig,
     timeout: config.timeout,
-    search_type: config.search_type
+    search_type: config.search_type,
+    model: config.model,
+    temperature: config.temperature,
+    maxTokens: config.maxTokens,
+    maxTurns: config.maxTurns,
+    useBuiltinTools: config.useBuiltinTools
   });
   return processAgentResult(result, parsed.search_term, config.morphApiKey, config.morphApiUrl);
 }
 async function processAgentResult(result, searchTerm, apiKey, apiUrl) {
+  const timings = result.timings;
   const finish = result.finish;
   if (result.terminationReason !== "completed" || !finish?.metadata) {
     const errorDetails = result.errors?.map((e) => e.message).join("; ") || "unknown reason";
     console.error(`[warp_grep] processAgentResult failed. Reason: ${result.terminationReason}. Errors: ${errorDetails}. Turns: ${result.timings?.turns?.length ?? 0}`);
-    return { success: false, error: `Search did not complete: ${errorDetails}` };
+    return { success: false, error: `Search did not complete: ${errorDetails}`, ...timings ? { timings } : {} };
   }
   const contexts = (finish.resolved ?? []).map((r) => ({
     file: r.path,
@@ -3848,12 +3878,12 @@ async function processAgentResult(result, searchTerm, apiKey, apiUrl) {
   } catch (error) {
     console.error(`[warp_grep] Failed to compact result contexts:`, error);
   }
-  return { success: true, contexts, summary: finish.payload };
+  return { success: true, contexts, summary: finish.payload, ...timings ? { timings } : {} };
 }
 async function* executeToolCallStreaming(input, config) {
   const parsed = typeof input === "string" ? JSON.parse(input) : input;
   const allowNames = config.search_type === "node_modules" ? ["node_modules"] : void 0;
-  const provider = config.remoteCommands ? new RemoteCommandsProvider(config.repoRoot, config.remoteCommands) : config.provider ?? await getLocalProvider(config.repoRoot, config.excludes, allowNames);
+  const provider = config.remoteCommands ? new RemoteCommandsProvider(config.repoRoot, config.remoteCommands) : config.provider ?? await getLocalProvider(config.repoRoot, config.excludes, allowNames, config.includes);
   const generator = runWarpGrepStreaming({
     searchTerm: parsed.search_term,
     repoRoot: config.repoRoot,
@@ -3865,7 +3895,12 @@ async function* executeToolCallStreaming(input, config) {
     morphApiUrl: config.morphApiUrl,
     retryConfig: config.retryConfig,
     timeout: config.timeout,
-    search_type: config.search_type
+    search_type: config.search_type,
+    model: config.model,
+    temperature: config.temperature,
+    maxTokens: config.maxTokens,
+    maxTurns: config.maxTurns,
+    useBuiltinTools: config.useBuiltinTools
   });
   let agentResult;
   for (; ; ) {
@@ -4423,7 +4458,7 @@ var MorphGit = class {
     const remotes = await import_isomorphic_git.default.listRemotes({ fs: import_fs2.default, dir });
     const originRemote = remotes.find((r) => r.remote === remote);
     if (originRemote) {
-      const match = originRemote.url.match(/\/repos\/([^\/]+)$/);
+      const match = originRemote.url.match(/\/repos\/([^/]+)$/);
       if (match) {
         repoId = match[1];
       }
diff --git a/dist/edge.cjs b/dist/edge.cjs
index 93517658747a9c903175c563b0dbb266aac6dd43..94382fcaf8e479e8532c714fcbd67a420dca1aa1 100644
--- a/dist/edge.cjs
+++ b/dist/edge.cjs
@@ -376,8 +376,7 @@ function countChanges(original, modified) {
 async function callMorphAPI(originalCode, codeEdit, instructions, filepath, config) {
   const apiKey = config.morphApiKey || (typeof process !== "undefined" ? process.env?.MORPH_API_KEY : void 0);
   const apiUrl = config.morphApiUrl || DEFAULT_API_URL;
-  const useLarge = config.large ?? (typeof process !== "undefined" ? process.env?.MORPH_LARGE_APPLY !== "false" : true);
-  const model = useLarge ? "morph-v3-large" : "morph-v3-fast";
+  const model = config.model ?? (config.large !== void 0 ? config.large ? "morph-v3-large" : "morph-v3-fast" : typeof process !== "undefined" ? process.env?.MORPH_APPLY_MODEL ?? (process.env?.MORPH_LARGE_APPLY !== void 0 ? process.env.MORPH_LARGE_APPLY !== "false" ? "morph-v3-large" : "morph-v3-fast" : "auto") : "auto");
   const timeout = config.timeout || DEFAULT_TIMEOUT;
   const debug = config.debug || false;
   if (!apiKey) {
diff --git a/dist/git/client.cjs b/dist/git/client.cjs
index cd58ffa251bc28997fe37c0ce143591293c0a0b3..fddaa51e0a436bfd98d0e9a8531fa4fb786a2a0f 100644
--- a/dist/git/client.cjs
+++ b/dist/git/client.cjs
@@ -152,7 +152,7 @@ var MorphGit = class {
     const remotes = await import_isomorphic_git.default.listRemotes({ fs: import_fs.default, dir });
     const originRemote = remotes.find((r) => r.remote === remote);
     if (originRemote) {
-      const match = originRemote.url.match(/\/repos\/([^\/]+)$/);
+      const match = originRemote.url.match(/\/repos\/([^/]+)$/);
       if (match) {
         repoId = match[1];
       }
diff --git a/dist/git/index.cjs b/dist/git/index.cjs
index 26fa50fea7dcd08338a1c99196bce9ce5fb215fd..90a9c3db7cc506df4992d62ac3f52421b1a26fbd 100644
--- a/dist/git/index.cjs
+++ b/dist/git/index.cjs
@@ -156,7 +156,7 @@ var MorphGit = class {
     const remotes = await import_isomorphic_git.default.listRemotes({ fs: import_fs.default, dir });
     const originRemote = remotes.find((r) => r.remote === remote);
     if (originRemote) {
-      const match = originRemote.url.match(/\/repos\/([^\/]+)$/);
+      const match = originRemote.url.match(/\/repos\/([^/]+)$/);
       if (match) {
         repoId = match[1];
       }
diff --git a/dist/index.cjs b/dist/index.cjs
index 780e51774632d7d73dcaa91ee48dcff99d0a4a48..e5dd01fe2d6468c4843461db3fa8b14e6a351097 100644
--- a/dist/index.cjs
+++ b/dist/index.cjs
@@ -382,8 +382,7 @@ function countChanges(original, modified) {
 async function callMorphAPI(originalCode, codeEdit, instructions, filepath, config) {
   const apiKey = config.morphApiKey || (typeof process !== "undefined" ? process.env?.MORPH_API_KEY : void 0);
   const apiUrl = config.morphApiUrl || DEFAULT_API_URL;
-  const useLarge = config.large ?? (typeof process !== "undefined" ? process.env?.MORPH_LARGE_APPLY !== "false" : true);
-  const model = useLarge ? "morph-v3-large" : "morph-v3-fast";
+  const model = config.model ?? (config.large !== void 0 ? config.large ? "morph-v3-large" : "morph-v3-fast" : typeof process !== "undefined" ? process.env?.MORPH_APPLY_MODEL ?? (process.env?.MORPH_LARGE_APPLY !== void 0 ? process.env.MORPH_LARGE_APPLY !== "false" ? "morph-v3-large" : "morph-v3-fast" : "auto") : "auto");
   const timeout = config.timeout || DEFAULT_TIMEOUT;
   const debug = config.debug || false;
   if (!apiKey) {
@@ -849,8 +848,12 @@ var init_local = __esm({
         if (options?.allowNames?.length) {
           this.allowNames = new Set(options.allowNames);
         }
+        if (options?.includes?.length) {
+          this.includes = [...options.includes];
+        }
       }
       allowNames;
+      includes;
       async grep(params) {
         let abs;
         try {
@@ -877,6 +880,7 @@ var init_local = __esm({
           contextLines,
           ...params.case_sensitive === false ? ["--ignore-case"] : [],
           ...params.glob ? ["--glob", params.glob] : [],
+          ...this.includes ? this.includes.flatMap((g) => ["-g", g]) : [],
           ...this.excludes.filter((e) => !this.allowNames?.has(e)).flatMap((e) => ["-g", `!${e}`]),
           params.pattern,
           targetArg || "."
@@ -1052,6 +1056,7 @@ Details: ${res.stderr}` : ""}`
           "--color=never",
           "-g",
           params.pattern,
+          ...this.includes ? this.includes.flatMap((g) => ["-g", g]) : [],
           ...this.excludes.filter((e) => !this.allowNames?.has(e)).flatMap((e) => ["-g", `!${e}`]),
           targetArg || "."
         ];
@@ -3067,6 +3072,9 @@ async function callModel(messages, model, options = {}) {
     defaultHeaders: { "X-Morph-SDK-Version": SDK_VERSION }
   });
   const MAX_EMPTY_RETRIES = 1;
+  const temperature = options.temperature ?? 0;
+  const maxTokens = options.maxTokens ?? 2048;
+  const useBuiltinTools = options.useBuiltinTools === true;
   logger.debug("WarpGrep", "model_call", { url: baseURL, model, message_count: messages.length });
   const callStartTime = Date.now();
   for (let attempt = 0; attempt <= MAX_EMPTY_RETRIES; attempt++) {
@@ -3074,10 +3082,10 @@ async function callModel(messages, model, options = {}) {
     try {
       data = await client.chat.completions.create({
         model,
-        temperature: 0,
-        max_tokens: 2048,
+        temperature,
+        max_tokens: maxTokens,
         messages,
-        tools: TOOL_SPECS,
+        ...useBuiltinTools ? {} : { tools: TOOL_SPECS },
         ...options.search_type ? { search_type: options.search_type } : {}
       });
     } catch (error) {
@@ -3178,8 +3186,11 @@ async function* runWarpGrepStreaming(config) {
   const timings = { turns: [], timeout_ms: timeoutMs };
   const repoRoot = config.repoRoot || process.cwd();
   const model = config.model || DEFAULT_MODEL;
+  const temperature = config.temperature;
+  const maxTokens = config.maxTokens;
+  const useBuiltinTools = config.useBuiltinTools === true;
   const messages = [];
-  const maxTurns = AGENT_CONFIG.MAX_TURNS;
+  const maxTurns = config.maxTurns ?? AGENT_CONFIG.MAX_TURNS;
   logger.debug("WarpGrep", "session_start", { search_term: config.searchTerm, model, max_turns: maxTurns });
   const initialStateStart = Date.now();
   const initialState = await buildInitialState(repoRoot, config.searchTerm, config.provider, { search_type: config.search_type });
@@ -3205,7 +3216,10 @@ async function* runWarpGrepStreaming(config) {
       morphApiUrl: config.morphApiUrl,
       retryConfig: config.retryConfig,
       timeout: timeoutMs,
-      search_type: config.search_type
+      search_type: config.search_type,
+      temperature,
+      maxTokens,
+      useBuiltinTools
     }).catch((e) => {
       const errMsg = e instanceof Error ? e.message : String(e);
       console.error(`[warp_grep] Morph API call failed on turn ${turn}:`, errMsg);
@@ -3583,12 +3597,12 @@ function createCodeStorageHttpCommands(config) {
 // tools/warp_grep/utils/github.ts
 function parseGitHubUrl(input) {
   input = input.trim();
-  const urlPattern = /^(?:https?:\/\/)?github\.com\/([^\/]+)\/([^\/\?\#]+)/;
+  const urlPattern = /^(?:https?:\/\/)?github\.com\/([^/]+)\/([^/?#]+)/;
   const match = input.match(urlPattern);
   if (match) {
     return { owner: match[1], repo: match[2].replace(/\.git$/, "") };
   }
-  const shortPattern = /^([^\/]+)\/([^\/]+)$/;
+  const shortPattern = /^([^/]+)\/([^/]+)$/;
   const shortMatch = input.match(shortPattern);
   if (shortMatch && !shortMatch[1].includes(".")) {
     return { owner: shortMatch[1], repo: shortMatch[2].replace(/\.git$/, "") };
@@ -3725,9 +3739,14 @@ async function compactFileContexts(files, query, apiKey, apiUrl, maxTokens = 1e4
 
 // tools/warp_grep/client.ts
 var DEFAULT_CODE_SEARCH_URL = "https://morphllm.com";
-async function getLocalProvider(repoRoot, excludes, allowNames) {
+async function getLocalProvider(repoRoot, excludes, allowNames, includes) {
   const { LocalRipgrepProvider: LocalRipgrepProvider2 } = await Promise.resolve().then(() => (init_local(), local_exports));
-  const opts = allowNames?.length ? { allowNames } : void 0;
+  const hasAllowNames = allowNames?.length;
+  const hasIncludes = includes?.length;
+  const opts = hasAllowNames || hasIncludes ? {
+    ...hasAllowNames ? { allowNames } : {},
+    ...hasIncludes ? { includes } : {}
+  } : void 0;
   return new LocalRipgrepProvider2(repoRoot, excludes, opts);
 }
 var WarpGrepClient = class {
@@ -3747,7 +3766,12 @@ var WarpGrepClient = class {
       morphApiUrl: this.config.morphApiUrl,
       retryConfig: this.config.retryConfig,
       timeout: this.config.timeout,
-      search_type: input.search_type
+      search_type: input.search_type,
+      model: input.model ?? this.config.model,
+      temperature: input.temperature ?? this.config.temperature,
+      maxTokens: input.maxTokens ?? this.config.maxTokens,
+      maxTurns: input.maxTurns ?? this.config.maxTurns,
+      useBuiltinTools: input.useBuiltinTools ?? this.config.useBuiltinTools
     };
     if (input.streamSteps) {
       return executeToolCallStreaming({ search_term: input.searchTerm }, toolConfig);
@@ -3835,7 +3859,7 @@ var WarpGrepClient = class {
 async function executeToolCall(input, config) {
   const parsed = typeof input === "string" ? JSON.parse(input) : input;
   const allowNames = config.search_type === "node_modules" ? ["node_modules"] : void 0;
-  const provider = config.remoteCommands ? new RemoteCommandsProvider(config.repoRoot, config.remoteCommands) : config.provider ?? await getLocalProvider(config.repoRoot, config.excludes, allowNames);
+  const provider = config.remoteCommands ? new RemoteCommandsProvider(config.repoRoot, config.remoteCommands) : config.provider ?? await getLocalProvider(config.repoRoot, config.excludes, allowNames, config.includes);
   const result = await runWarpGrep({
     searchTerm: parsed.search_term,
     repoRoot: config.repoRoot,
@@ -3847,16 +3871,22 @@ async function executeToolCall(input, config) {
     morphApiUrl: config.morphApiUrl,
     retryConfig: config.retryConfig,
     timeout: config.timeout,
-    search_type: config.search_type
+    search_type: config.search_type,
+    model: config.model,
+    temperature: config.temperature,
+    maxTokens: config.maxTokens,
+    maxTurns: config.maxTurns,
+    useBuiltinTools: config.useBuiltinTools
   });
   return processAgentResult(result, parsed.search_term, config.morphApiKey, config.morphApiUrl);
 }
 async function processAgentResult(result, searchTerm, apiKey, apiUrl) {
+  const timings = result.timings;
   const finish = result.finish;
   if (result.terminationReason !== "completed" || !finish?.metadata) {
     const errorDetails = result.errors?.map((e) => e.message).join("; ") || "unknown reason";
     console.error(`[warp_grep] processAgentResult failed. Reason: ${result.terminationReason}. Errors: ${errorDetails}. Turns: ${result.timings?.turns?.length ?? 0}`);
-    return { success: false, error: `Search did not complete: ${errorDetails}` };
+    return { success: false, error: `Search did not complete: ${errorDetails}`, ...timings ? { timings } : {} };
   }
   const contexts = (finish.resolved ?? []).map((r) => ({
     file: r.path,
@@ -3872,12 +3902,12 @@ async function processAgentResult(result, searchTerm, apiKey, apiUrl) {
   } catch (error) {
     console.error(`[warp_grep] Failed to compact result contexts:`, error);
   }
-  return { success: true, contexts, summary: finish.payload };
+  return { success: true, contexts, summary: finish.payload, ...timings ? { timings } : {} };
 }
 async function* executeToolCallStreaming(input, config) {
   const parsed = typeof input === "string" ? JSON.parse(input) : input;
   const allowNames = config.search_type === "node_modules" ? ["node_modules"] : void 0;
-  const provider = config.remoteCommands ? new RemoteCommandsProvider(config.repoRoot, config.remoteCommands) : config.provider ?? await getLocalProvider(config.repoRoot, config.excludes, allowNames);
+  const provider = config.remoteCommands ? new RemoteCommandsProvider(config.repoRoot, config.remoteCommands) : config.provider ?? await getLocalProvider(config.repoRoot, config.excludes, allowNames, config.includes);
   const generator = runWarpGrepStreaming({
     searchTerm: parsed.search_term,
     repoRoot: config.repoRoot,
@@ -3889,7 +3919,12 @@ async function* executeToolCallStreaming(input, config) {
     morphApiUrl: config.morphApiUrl,
     retryConfig: config.retryConfig,
     timeout: config.timeout,
-    search_type: config.search_type
+    search_type: config.search_type,
+    model: config.model,
+    temperature: config.temperature,
+    maxTokens: config.maxTokens,
+    maxTurns: config.maxTurns,
+    useBuiltinTools: config.useBuiltinTools
   });
   let agentResult;
   for (; ; ) {
@@ -4447,7 +4482,7 @@ var MorphGit = class {
     const remotes = await import_isomorphic_git.default.listRemotes({ fs: import_fs2.default, dir });
     const originRemote = remotes.find((r) => r.remote === remote);
     if (originRemote) {
-      const match = originRemote.url.match(/\/repos\/([^\/]+)$/);
+      const match = originRemote.url.match(/\/repos\/([^/]+)$/);
       if (match) {
         repoId = match[1];
       }
diff --git a/dist/subagents/anthropic.cjs b/dist/subagents/anthropic.cjs
index bbd0a3f74f79fe60d845293e5097d5a9615fe60d..6ed86ee45fe87ac7aa12996cf9407f27525ede1d 100644
--- a/dist/subagents/anthropic.cjs
+++ b/dist/subagents/anthropic.cjs
@@ -1864,12 +1864,12 @@ function createCodeStorageHttpCommands(config) {
 // tools/warp_grep/utils/github.ts
 function parseGitHubUrl(input) {
   input = input.trim();
-  const urlPattern = /^(?:https?:\/\/)?github\.com\/([^\/]+)\/([^\/\?\#]+)/;
+  const urlPattern = /^(?:https?:\/\/)?github\.com\/([^/]+)\/([^/?#]+)/;
   const match = input.match(urlPattern);
   if (match) {
     return { owner: match[1], repo: match[2].replace(/\.git$/, "") };
   }
-  const shortPattern = /^([^\/]+)\/([^\/]+)$/;
+  const shortPattern = /^([^/]+)\/([^/]+)$/;
   const shortMatch = input.match(shortPattern);
   if (shortMatch && !shortMatch[1].includes(".")) {
     return { owner: shortMatch[1], repo: shortMatch[2].replace(/\.git$/, "") };
diff --git a/dist/subagents/vercel.cjs b/dist/subagents/vercel.cjs
index e351a1c8705aac29eb35fb73a06231104af19ee7..ba215cae5efc2cee459446a041e720130bc1e9e4 100644
--- a/dist/subagents/vercel.cjs
+++ b/dist/subagents/vercel.cjs
@@ -1866,12 +1866,12 @@ function createCodeStorageHttpCommands(config) {
 // tools/warp_grep/utils/github.ts
 function parseGitHubUrl(input) {
   input = input.trim();
-  const urlPattern = /^(?:https?:\/\/)?github\.com\/([^\/]+)\/([^\/\?\#]+)/;
+  const urlPattern = /^(?:https?:\/\/)?github\.com\/([^/]+)\/([^/?#]+)/;
   const match = input.match(urlPattern);
   if (match) {
     return { owner: match[1], repo: match[2].replace(/\.git$/, "") };
   }
-  const shortPattern = /^([^\/]+)\/([^\/]+)$/;
+  const shortPattern = /^([^/]+)\/([^/]+)$/;
   const shortMatch = input.match(shortPattern);
   if (shortMatch && !shortMatch[1].includes(".")) {
     return { owner: shortMatch[1], repo: shortMatch[2].replace(/\.git$/, "") };
diff --git a/dist/tools/fastapply/anthropic.cjs b/dist/tools/fastapply/anthropic.cjs
index 62738a0263162b95d7946259c23b7cf0e221f3f2..c2216a0d0a4cbd3cff92684e1848081e992d67bf 100644
--- a/dist/tools/fastapply/anthropic.cjs
+++ b/dist/tools/fastapply/anthropic.cjs
@@ -382,8 +382,7 @@ function countChanges(original, modified) {
 async function callMorphAPI(originalCode, codeEdit, instructions, filepath, config) {
   const apiKey = config.morphApiKey || (typeof process !== "undefined" ? process.env?.MORPH_API_KEY : void 0);
   const apiUrl = config.morphApiUrl || DEFAULT_API_URL;
-  const useLarge = config.large ?? (typeof process !== "undefined" ? process.env?.MORPH_LARGE_APPLY !== "false" : true);
-  const model = useLarge ? "morph-v3-large" : "morph-v3-fast";
+  const model = config.model ?? (config.large !== void 0 ? config.large ? "morph-v3-large" : "morph-v3-fast" : typeof process !== "undefined" ? process.env?.MORPH_APPLY_MODEL ?? (process.env?.MORPH_LARGE_APPLY !== void 0 ? process.env.MORPH_LARGE_APPLY !== "false" ? "morph-v3-large" : "morph-v3-fast" : "auto") : "auto");
   const timeout = config.timeout || DEFAULT_TIMEOUT;
   const debug = config.debug || false;
   if (!apiKey) {
diff --git a/dist/tools/fastapply/apply.cjs b/dist/tools/fastapply/apply.cjs
index 2cd7b70233376285a5ec64e931eec44784a6494c..0c333be6ce429ad8af48123d51e9d15b2c180bff 100644
--- a/dist/tools/fastapply/apply.cjs
+++ b/dist/tools/fastapply/apply.cjs
@@ -368,8 +368,7 @@ function countChanges(original, modified) {
 async function callMorphAPI(originalCode, codeEdit, instructions, filepath, config) {
   const apiKey = config.morphApiKey || (typeof process !== "undefined" ? process.env?.MORPH_API_KEY : void 0);
   const apiUrl = config.morphApiUrl || DEFAULT_API_URL;
-  const useLarge = config.large ?? (typeof process !== "undefined" ? process.env?.MORPH_LARGE_APPLY !== "false" : true);
-  const model = useLarge ? "morph-v3-large" : "morph-v3-fast";
+  const model = config.model ?? (config.large !== void 0 ? config.large ? "morph-v3-large" : "morph-v3-fast" : typeof process !== "undefined" ? process.env?.MORPH_APPLY_MODEL ?? (process.env?.MORPH_LARGE_APPLY !== void 0 ? process.env.MORPH_LARGE_APPLY !== "false" ? "morph-v3-large" : "morph-v3-fast" : "auto") : "auto");
   const timeout = config.timeout || DEFAULT_TIMEOUT;
   const debug = config.debug || false;
   if (!apiKey) {
diff --git a/dist/tools/fastapply/core.cjs b/dist/tools/fastapply/core.cjs
index c14b9dc0a056c3b7bdbdb85b37bd0ea05b2ed880..72be7c779a9acf89425bd7edabd043b28e9103b5 100644
--- a/dist/tools/fastapply/core.cjs
+++ b/dist/tools/fastapply/core.cjs
@@ -382,8 +382,7 @@ function countChanges(original, modified) {
 async function callMorphAPI(originalCode, codeEdit, instructions, filepath, config) {
   const apiKey = config.morphApiKey || (typeof process !== "undefined" ? process.env?.MORPH_API_KEY : void 0);
   const apiUrl = config.morphApiUrl || DEFAULT_API_URL;
-  const useLarge = config.large ?? (typeof process !== "undefined" ? process.env?.MORPH_LARGE_APPLY !== "false" : true);
-  const model = useLarge ? "morph-v3-large" : "morph-v3-fast";
+  const model = config.model ?? (config.large !== void 0 ? config.large ? "morph-v3-large" : "morph-v3-fast" : typeof process !== "undefined" ? process.env?.MORPH_APPLY_MODEL ?? (process.env?.MORPH_LARGE_APPLY !== void 0 ? process.env.MORPH_LARGE_APPLY !== "false" ? "morph-v3-large" : "morph-v3-fast" : "auto") : "auto");
   const timeout = config.timeout || DEFAULT_TIMEOUT;
   const debug = config.debug || false;
   if (!apiKey) {
diff --git a/dist/tools/fastapply/index.cjs b/dist/tools/fastapply/index.cjs
index 8aaab3e53b6ba399d9e535f457582bcbbec6ee4f..8844930799a283050db2790279b5a0aa426b114e 100644
--- a/dist/tools/fastapply/index.cjs
+++ b/dist/tools/fastapply/index.cjs
@@ -382,8 +382,7 @@ function countChanges(original, modified) {
 async function callMorphAPI(originalCode, codeEdit, instructions, filepath, config) {
   const apiKey = config.morphApiKey || (typeof process !== "undefined" ? process.env?.MORPH_API_KEY : void 0);
   const apiUrl = config.morphApiUrl || DEFAULT_API_URL;
-  const useLarge = config.large ?? (typeof process !== "undefined" ? process.env?.MORPH_LARGE_APPLY !== "false" : true);
-  const model = useLarge ? "morph-v3-large" : "morph-v3-fast";
+  const model = config.model ?? (config.large !== void 0 ? config.large ? "morph-v3-large" : "morph-v3-fast" : typeof process !== "undefined" ? process.env?.MORPH_APPLY_MODEL ?? (process.env?.MORPH_LARGE_APPLY !== void 0 ? process.env.MORPH_LARGE_APPLY !== "false" ? "morph-v3-large" : "morph-v3-fast" : "auto") : "auto");
   const timeout = config.timeout || DEFAULT_TIMEOUT;
   const debug = config.debug || false;
   if (!apiKey) {
diff --git a/dist/tools/fastapply/openai.cjs b/dist/tools/fastapply/openai.cjs
index 3f14d691a6ab0b2849bcfdbbaf25366a6eeb82f8..1b4560621450af83158bb0a13f2c8809d2ac3f25 100644
--- a/dist/tools/fastapply/openai.cjs
+++ b/dist/tools/fastapply/openai.cjs
@@ -382,8 +382,7 @@ function countChanges(original, modified) {
 async function callMorphAPI(originalCode, codeEdit, instructions, filepath, config) {
   const apiKey = config.morphApiKey || (typeof process !== "undefined" ? process.env?.MORPH_API_KEY : void 0);
   const apiUrl = config.morphApiUrl || DEFAULT_API_URL;
-  const useLarge = config.large ?? (typeof process !== "undefined" ? process.env?.MORPH_LARGE_APPLY !== "false" : true);
-  const model = useLarge ? "morph-v3-large" : "morph-v3-fast";
+  const model = config.model ?? (config.large !== void 0 ? config.large ? "morph-v3-large" : "morph-v3-fast" : typeof process !== "undefined" ? process.env?.MORPH_APPLY_MODEL ?? (process.env?.MORPH_LARGE_APPLY !== void 0 ? process.env.MORPH_LARGE_APPLY !== "false" ? "morph-v3-large" : "morph-v3-fast" : "auto") : "auto");
   const timeout = config.timeout || DEFAULT_TIMEOUT;
   const debug = config.debug || false;
   if (!apiKey) {
diff --git a/dist/tools/fastapply/types.d.ts b/dist/tools/fastapply/types.d.ts
index 21cc80fba27bc1b315dd6b4b2259d5faba62d593..0c41f7ba29ebe70eeee9f293592c53278d32af5f 100644
--- a/dist/tools/fastapply/types.d.ts
+++ b/dist/tools/fastapply/types.d.ts
@@ -50,7 +50,9 @@ interface EditFileConfig {
     morphApiKey?: string;
     /** Morph API base URL */
     morphApiUrl?: string;
-    /** Use morph-v3-large (default: true). Set to false to use morph-v3-fast. Defaults to MORPH_LARGE_APPLY env var. */
+    /** Apply model to use (default: auto). Overrides large when provided. */
+    model?: 'auto' | 'morph-v3-fast' | 'morph-v3-large';
+    /** Use morph-v3-large when true, morph-v3-fast when false. Deprecated: prefer model. */
     large?: boolean;
     /** Base directory for file operations (defaults to cwd) */
     baseDir?: string;
@@ -104,7 +106,9 @@ interface ApplyEditConfig {
     morphApiKey?: string;
     /** Morph API base URL */
     morphApiUrl?: string;
-    /** Use morph-v3-large (default: true). Set to false to use morph-v3-fast. Defaults to MORPH_LARGE_APPLY env var. */
+    /** Apply model to use (default: auto). Overrides large when provided. */
+    model?: 'auto' | 'morph-v3-fast' | 'morph-v3-large';
+    /** Use morph-v3-large when true, morph-v3-fast when false. Deprecated: prefer model. */
     large?: boolean;
     /** Whether to generate udiff (default: true) */
     generateUdiff?: boolean;
diff --git a/dist/tools/fastapply/vercel.cjs b/dist/tools/fastapply/vercel.cjs
index 7088b2dccb57e69b80c61fff13705adacafa8ab4..2fbbb85d9e24e762e1596397a17877cba2e316ca 100644
--- a/dist/tools/fastapply/vercel.cjs
+++ b/dist/tools/fastapply/vercel.cjs
@@ -382,8 +382,7 @@ function countChanges(original, modified) {
 async function callMorphAPI(originalCode, codeEdit, instructions, filepath, config) {
   const apiKey = config.morphApiKey || (typeof process !== "undefined" ? process.env?.MORPH_API_KEY : void 0);
   const apiUrl = config.morphApiUrl || DEFAULT_API_URL;
-  const useLarge = config.large ?? (typeof process !== "undefined" ? process.env?.MORPH_LARGE_APPLY !== "false" : true);
-  const model = useLarge ? "morph-v3-large" : "morph-v3-fast";
+  const model = config.model ?? (config.large !== void 0 ? config.large ? "morph-v3-large" : "morph-v3-fast" : typeof process !== "undefined" ? process.env?.MORPH_APPLY_MODEL ?? (process.env?.MORPH_LARGE_APPLY !== void 0 ? process.env.MORPH_LARGE_APPLY !== "false" ? "morph-v3-large" : "morph-v3-fast" : "auto") : "auto");
   const timeout = config.timeout || DEFAULT_TIMEOUT;
   const debug = config.debug || false;
   if (!apiKey) {
diff --git a/dist/tools/index.cjs b/dist/tools/index.cjs
index 7f81529df36213d72a97700253cb8b4dee78f038..5464a9e76a805b23c6b5352ec3386cd94b20080d 100644
--- a/dist/tools/index.cjs
+++ b/dist/tools/index.cjs
@@ -382,8 +382,7 @@ function countChanges(original, modified) {
 async function callMorphAPI(originalCode, codeEdit, instructions, filepath, config) {
   const apiKey = config.morphApiKey || (typeof process !== "undefined" ? process.env?.MORPH_API_KEY : void 0);
   const apiUrl = config.morphApiUrl || DEFAULT_API_URL;
-  const useLarge = config.large ?? (typeof process !== "undefined" ? process.env?.MORPH_LARGE_APPLY !== "false" : true);
-  const model = useLarge ? "morph-v3-large" : "morph-v3-fast";
+  const model = config.model ?? (config.large !== void 0 ? config.large ? "morph-v3-large" : "morph-v3-fast" : typeof process !== "undefined" ? process.env?.MORPH_APPLY_MODEL ?? (process.env?.MORPH_LARGE_APPLY !== void 0 ? process.env.MORPH_LARGE_APPLY !== "false" ? "morph-v3-large" : "morph-v3-fast" : "auto") : "auto");
   const timeout = config.timeout || DEFAULT_TIMEOUT;
   const debug = config.debug || false;
   if (!apiKey) {
diff --git a/dist/tools/warp_grep/anthropic.cjs b/dist/tools/warp_grep/anthropic.cjs
index 1a5a61d4dcd1989564f304691e7ced252872061f..156282e80925a89232ed8b8da4aa9b5e3ae83a3c 100644
--- a/dist/tools/warp_grep/anthropic.cjs
+++ b/dist/tools/warp_grep/anthropic.cjs
@@ -388,8 +388,12 @@ var init_local = __esm({
         if (options?.allowNames?.length) {
           this.allowNames = new Set(options.allowNames);
         }
+        if (options?.includes?.length) {
+          this.includes = [...options.includes];
+        }
       }
       allowNames;
+      includes;
       async grep(params) {
         let abs;
         try {
@@ -416,6 +420,7 @@ var init_local = __esm({
           contextLines,
           ...params.case_sensitive === false ? ["--ignore-case"] : [],
           ...params.glob ? ["--glob", params.glob] : [],
+          ...this.includes ? this.includes.flatMap((g) => ["-g", g]) : [],
           ...this.excludes.filter((e) => !this.allowNames?.has(e)).flatMap((e) => ["-g", `!${e}`]),
           params.pattern,
           targetArg || "."
@@ -591,6 +596,7 @@ Details: ${res.stderr}` : ""}`
           "--color=never",
           "-g",
           params.pattern,
+          ...this.includes ? this.includes.flatMap((g) => ["-g", g]) : [],
           ...this.excludes.filter((e) => !this.allowNames?.has(e)).flatMap((e) => ["-g", `!${e}`]),
           targetArg || "."
         ];
@@ -1356,6 +1362,9 @@ async function callModel(messages, model, options = {}) {
     defaultHeaders: { "X-Morph-SDK-Version": SDK_VERSION }
   });
   const MAX_EMPTY_RETRIES = 1;
+  const temperature = options.temperature ?? 0;
+  const maxTokens = options.maxTokens ?? 2048;
+  const useBuiltinTools = options.useBuiltinTools === true;
   logger.debug("WarpGrep", "model_call", { url: baseURL, model, message_count: messages.length });
   const callStartTime = Date.now();
   for (let attempt = 0; attempt <= MAX_EMPTY_RETRIES; attempt++) {
@@ -1363,10 +1372,10 @@ async function callModel(messages, model, options = {}) {
     try {
       data = await client.chat.completions.create({
         model,
-        temperature: 0,
-        max_tokens: 2048,
+        temperature,
+        max_tokens: maxTokens,
         messages,
-        tools: TOOL_SPECS,
+        ...useBuiltinTools ? {} : { tools: TOOL_SPECS },
         ...options.search_type ? { search_type: options.search_type } : {}
       });
     } catch (error) {
@@ -1467,8 +1476,11 @@ async function* runWarpGrepStreaming(config) {
   const timings = { turns: [], timeout_ms: timeoutMs };
   const repoRoot = config.repoRoot || process.cwd();
   const model = config.model || DEFAULT_MODEL;
+  const temperature = config.temperature;
+  const maxTokens = config.maxTokens;
+  const useBuiltinTools = config.useBuiltinTools === true;
   const messages = [];
-  const maxTurns = AGENT_CONFIG.MAX_TURNS;
+  const maxTurns = config.maxTurns ?? AGENT_CONFIG.MAX_TURNS;
   logger.debug("WarpGrep", "session_start", { search_term: config.searchTerm, model, max_turns: maxTurns });
   const initialStateStart = Date.now();
   const initialState = await buildInitialState(repoRoot, config.searchTerm, config.provider, { search_type: config.search_type });
@@ -1494,7 +1506,10 @@ async function* runWarpGrepStreaming(config) {
       morphApiUrl: config.morphApiUrl,
       retryConfig: config.retryConfig,
       timeout: timeoutMs,
-      search_type: config.search_type
+      search_type: config.search_type,
+      temperature,
+      maxTokens,
+      useBuiltinTools
     }).catch((e) => {
       const errMsg = e instanceof Error ? e.message : String(e);
       console.error(`[warp_grep] Morph API call failed on turn ${turn}:`, errMsg);
@@ -1869,12 +1884,12 @@ function createCodeStorageHttpCommands(config) {
 // tools/warp_grep/utils/github.ts
 function parseGitHubUrl(input) {
   input = input.trim();
-  const urlPattern = /^(?:https?:\/\/)?github\.com\/([^\/]+)\/([^\/\?\#]+)/;
+  const urlPattern = /^(?:https?:\/\/)?github\.com\/([^/]+)\/([^/?#]+)/;
   const match = input.match(urlPattern);
   if (match) {
     return { owner: match[1], repo: match[2].replace(/\.git$/, "") };
   }
-  const shortPattern = /^([^\/]+)\/([^\/]+)$/;
+  const shortPattern = /^([^/]+)\/([^/]+)$/;
   const shortMatch = input.match(shortPattern);
   if (shortMatch && !shortMatch[1].includes(".")) {
     return { owner: shortMatch[1], repo: shortMatch[2].replace(/\.git$/, "") };
@@ -2011,9 +2026,14 @@ async function compactFileContexts(files, query, apiKey, apiUrl, maxTokens = 1e4
 
 // tools/warp_grep/client.ts
 var DEFAULT_CODE_SEARCH_URL = "https://morphllm.com";
-async function getLocalProvider(repoRoot, excludes, allowNames) {
+async function getLocalProvider(repoRoot, excludes, allowNames, includes) {
   const { LocalRipgrepProvider: LocalRipgrepProvider2 } = await Promise.resolve().then(() => (init_local(), local_exports));
-  const opts = allowNames?.length ? { allowNames } : void 0;
+  const hasAllowNames = allowNames?.length;
+  const hasIncludes = includes?.length;
+  const opts = hasAllowNames || hasIncludes ? {
+    ...hasAllowNames ? { allowNames } : {},
+    ...hasIncludes ? { includes } : {}
+  } : void 0;
   return new LocalRipgrepProvider2(repoRoot, excludes, opts);
 }
 var WarpGrepClient = class {
@@ -2033,7 +2053,12 @@ var WarpGrepClient = class {
       morphApiUrl: this.config.morphApiUrl,
       retryConfig: this.config.retryConfig,
       timeout: this.config.timeout,
-      search_type: input.search_type
+      search_type: input.search_type,
+      model: input.model ?? this.config.model,
+      temperature: input.temperature ?? this.config.temperature,
+      maxTokens: input.maxTokens ?? this.config.maxTokens,
+      maxTurns: input.maxTurns ?? this.config.maxTurns,
+      useBuiltinTools: input.useBuiltinTools ?? this.config.useBuiltinTools
     };
     if (input.streamSteps) {
       return executeToolCallStreaming({ search_term: input.searchTerm }, toolConfig);
@@ -2121,7 +2146,7 @@ var WarpGrepClient = class {
 async function executeToolCall(input, config) {
   const parsed = typeof input === "string" ? JSON.parse(input) : input;
   const allowNames = config.search_type === "node_modules" ? ["node_modules"] : void 0;
-  const provider = config.remoteCommands ? new RemoteCommandsProvider(config.repoRoot, config.remoteCommands) : config.provider ?? await getLocalProvider(config.repoRoot, config.excludes, allowNames);
+  const provider = config.remoteCommands ? new RemoteCommandsProvider(config.repoRoot, config.remoteCommands) : config.provider ?? await getLocalProvider(config.repoRoot, config.excludes, allowNames, config.includes);
   const result = await runWarpGrep({
     searchTerm: parsed.search_term,
     repoRoot: config.repoRoot,
@@ -2133,16 +2158,22 @@ async function executeToolCall(input, config) {
     morphApiUrl: config.morphApiUrl,
     retryConfig: config.retryConfig,
     timeout: config.timeout,
-    search_type: config.search_type
+    search_type: config.search_type,
+    model: config.model,
+    temperature: config.temperature,
+    maxTokens: config.maxTokens,
+    maxTurns: config.maxTurns,
+    useBuiltinTools: config.useBuiltinTools
   });
   return processAgentResult(result, parsed.search_term, config.morphApiKey, config.morphApiUrl);
 }
 async function processAgentResult(result, searchTerm, apiKey, apiUrl) {
+  const timings = result.timings;
   const finish = result.finish;
   if (result.terminationReason !== "completed" || !finish?.metadata) {
     const errorDetails = result.errors?.map((e) => e.message).join("; ") || "unknown reason";
     console.error(`[warp_grep] processAgentResult failed. Reason: ${result.terminationReason}. Errors: ${errorDetails}. Turns: ${result.timings?.turns?.length ?? 0}`);
-    return { success: false, error: `Search did not complete: ${errorDetails}` };
+    return { success: false, error: `Search did not complete: ${errorDetails}`, ...timings ? { timings } : {} };
   }
   const contexts = (finish.resolved ?? []).map((r) => ({
     file: r.path,
@@ -2158,12 +2189,12 @@ async function processAgentResult(result, searchTerm, apiKey, apiUrl) {
   } catch (error) {
     console.error(`[warp_grep] Failed to compact result contexts:`, error);
   }
-  return { success: true, contexts, summary: finish.payload };
+  return { success: true, contexts, summary: finish.payload, ...timings ? { timings } : {} };
 }
 async function* executeToolCallStreaming(input, config) {
   const parsed = typeof input === "string" ? JSON.parse(input) : input;
   const allowNames = config.search_type === "node_modules" ? ["node_modules"] : void 0;
-  const provider = config.remoteCommands ? new RemoteCommandsProvider(config.repoRoot, config.remoteCommands) : config.provider ?? await getLocalProvider(config.repoRoot, config.excludes, allowNames);
+  const provider = config.remoteCommands ? new RemoteCommandsProvider(config.repoRoot, config.remoteCommands) : config.provider ?? await getLocalProvider(config.repoRoot, config.excludes, allowNames, config.includes);
   const generator = runWarpGrepStreaming({
     searchTerm: parsed.search_term,
     repoRoot: config.repoRoot,
@@ -2175,7 +2206,12 @@ async function* executeToolCallStreaming(input, config) {
     morphApiUrl: config.morphApiUrl,
     retryConfig: config.retryConfig,
     timeout: config.timeout,
-    search_type: config.search_type
+    search_type: config.search_type,
+    model: config.model,
+    temperature: config.temperature,
+    maxTokens: config.maxTokens,
+    maxTurns: config.maxTurns,
+    useBuiltinTools: config.useBuiltinTools
   });
   let agentResult;
   for (; ; ) {
diff --git a/dist/tools/warp_grep/client.cjs b/dist/tools/warp_grep/client.cjs
index 80b355d51581d19987143c1ae3ef5b3a0ff5b421..1acfa95659a24a24a6cd93a533c70aecae07f8e9 100644
--- a/dist/tools/warp_grep/client.cjs
+++ b/dist/tools/warp_grep/client.cjs
@@ -388,8 +388,12 @@ var init_local = __esm({
         if (options?.allowNames?.length) {
           this.allowNames = new Set(options.allowNames);
         }
+        if (options?.includes?.length) {
+          this.includes = [...options.includes];
+        }
       }
       allowNames;
+      includes;
       async grep(params) {
         let abs;
         try {
@@ -416,6 +420,7 @@ var init_local = __esm({
           contextLines,
           ...params.case_sensitive === false ? ["--ignore-case"] : [],
           ...params.glob ? ["--glob", params.glob] : [],
+          ...this.includes ? this.includes.flatMap((g) => ["-g", g]) : [],
           ...this.excludes.filter((e) => !this.allowNames?.has(e)).flatMap((e) => ["-g", `!${e}`]),
           params.pattern,
           targetArg || "."
@@ -591,6 +596,7 @@ Details: ${res.stderr}` : ""}`
           "--color=never",
           "-g",
           params.pattern,
+          ...this.includes ? this.includes.flatMap((g) => ["-g", g]) : [],
           ...this.excludes.filter((e) => !this.allowNames?.has(e)).flatMap((e) => ["-g", `!${e}`]),
           targetArg || "."
         ];
@@ -1357,6 +1363,9 @@ async function callModel(messages, model, options = {}) {
     defaultHeaders: { "X-Morph-SDK-Version": SDK_VERSION }
   });
   const MAX_EMPTY_RETRIES = 1;
+  const temperature = options.temperature ?? 0;
+  const maxTokens = options.maxTokens ?? 2048;
+  const useBuiltinTools = options.useBuiltinTools === true;
   logger.debug("WarpGrep", "model_call", { url: baseURL, model, message_count: messages.length });
   const callStartTime = Date.now();
   for (let attempt = 0; attempt <= MAX_EMPTY_RETRIES; attempt++) {
@@ -1364,10 +1373,10 @@ async function callModel(messages, model, options = {}) {
     try {
       data = await client.chat.completions.create({
         model,
-        temperature: 0,
-        max_tokens: 2048,
+        temperature,
+        max_tokens: maxTokens,
         messages,
-        tools: TOOL_SPECS,
+        ...useBuiltinTools ? {} : { tools: TOOL_SPECS },
         ...options.search_type ? { search_type: options.search_type } : {}
       });
     } catch (error) {
@@ -1468,8 +1477,11 @@ async function* runWarpGrepStreaming(config) {
   const timings = { turns: [], timeout_ms: timeoutMs };
   const repoRoot = config.repoRoot || process.cwd();
   const model = config.model || DEFAULT_MODEL;
+  const temperature = config.temperature;
+  const maxTokens = config.maxTokens;
+  const useBuiltinTools = config.useBuiltinTools === true;
   const messages = [];
-  const maxTurns = AGENT_CONFIG.MAX_TURNS;
+  const maxTurns = config.maxTurns ?? AGENT_CONFIG.MAX_TURNS;
   logger.debug("WarpGrep", "session_start", { search_term: config.searchTerm, model, max_turns: maxTurns });
   const initialStateStart = Date.now();
   const initialState = await buildInitialState(repoRoot, config.searchTerm, config.provider, { search_type: config.search_type });
@@ -1495,7 +1507,10 @@ async function* runWarpGrepStreaming(config) {
       morphApiUrl: config.morphApiUrl,
       retryConfig: config.retryConfig,
       timeout: timeoutMs,
-      search_type: config.search_type
+      search_type: config.search_type,
+      temperature,
+      maxTokens,
+      useBuiltinTools
     }).catch((e) => {
       const errMsg = e instanceof Error ? e.message : String(e);
       console.error(`[warp_grep] Morph API call failed on turn ${turn}:`, errMsg);
@@ -1870,12 +1885,12 @@ function createCodeStorageHttpCommands(config) {
 // tools/warp_grep/utils/github.ts
 function parseGitHubUrl(input) {
   input = input.trim();
-  const urlPattern = /^(?:https?:\/\/)?github\.com\/([^\/]+)\/([^\/\?\#]+)/;
+  const urlPattern = /^(?:https?:\/\/)?github\.com\/([^/]+)\/([^/?#]+)/;
   const match = input.match(urlPattern);
   if (match) {
     return { owner: match[1], repo: match[2].replace(/\.git$/, "") };
   }
-  const shortPattern = /^([^\/]+)\/([^\/]+)$/;
+  const shortPattern = /^([^/]+)\/([^/]+)$/;
   const shortMatch = input.match(shortPattern);
   if (shortMatch && !shortMatch[1].includes(".")) {
     return { owner: shortMatch[1], repo: shortMatch[2].replace(/\.git$/, "") };
@@ -2012,9 +2027,14 @@ async function compactFileContexts(files, query, apiKey, apiUrl, maxTokens = 1e4
 
 // tools/warp_grep/client.ts
 var DEFAULT_CODE_SEARCH_URL = "https://morphllm.com";
-async function getLocalProvider(repoRoot, excludes, allowNames) {
+async function getLocalProvider(repoRoot, excludes, allowNames, includes) {
   const { LocalRipgrepProvider: LocalRipgrepProvider2 } = await Promise.resolve().then(() => (init_local(), local_exports));
-  const opts = allowNames?.length ? { allowNames } : void 0;
+  const hasAllowNames = allowNames?.length;
+  const hasIncludes = includes?.length;
+  const opts = hasAllowNames || hasIncludes ? {
+    ...hasAllowNames ? { allowNames } : {},
+    ...hasIncludes ? { includes } : {}
+  } : void 0;
   return new LocalRipgrepProvider2(repoRoot, excludes, opts);
 }
 var WarpGrepClient = class {
@@ -2034,7 +2054,12 @@ var WarpGrepClient = class {
       morphApiUrl: this.config.morphApiUrl,
       retryConfig: this.config.retryConfig,
       timeout: this.config.timeout,
-      search_type: input.search_type
+      search_type: input.search_type,
+      model: input.model ?? this.config.model,
+      temperature: input.temperature ?? this.config.temperature,
+      maxTokens: input.maxTokens ?? this.config.maxTokens,
+      maxTurns: input.maxTurns ?? this.config.maxTurns,
+      useBuiltinTools: input.useBuiltinTools ?? this.config.useBuiltinTools
     };
     if (input.streamSteps) {
       return executeToolCallStreaming({ search_term: input.searchTerm }, toolConfig);
@@ -2129,7 +2154,7 @@ function executeWarpGrep(input, config) {
 async function executeToolCall(input, config) {
   const parsed = typeof input === "string" ? JSON.parse(input) : input;
   const allowNames = config.search_type === "node_modules" ? ["node_modules"] : void 0;
-  const provider = config.remoteCommands ? new RemoteCommandsProvider(config.repoRoot, config.remoteCommands) : config.provider ?? await getLocalProvider(config.repoRoot, config.excludes, allowNames);
+  const provider = config.remoteCommands ? new RemoteCommandsProvider(config.repoRoot, config.remoteCommands) : config.provider ?? await getLocalProvider(config.repoRoot, config.excludes, allowNames, config.includes);
   const result = await runWarpGrep({
     searchTerm: parsed.search_term,
     repoRoot: config.repoRoot,
@@ -2141,16 +2166,22 @@ async function executeToolCall(input, config) {
     morphApiUrl: config.morphApiUrl,
     retryConfig: config.retryConfig,
     timeout: config.timeout,
-    search_type: config.search_type
+    search_type: config.search_type,
+    model: config.model,
+    temperature: config.temperature,
+    maxTokens: config.maxTokens,
+    maxTurns: config.maxTurns,
+    useBuiltinTools: config.useBuiltinTools
   });
   return processAgentResult(result, parsed.search_term, config.morphApiKey, config.morphApiUrl);
 }
 async function processAgentResult(result, searchTerm, apiKey, apiUrl) {
+  const timings = result.timings;
   const finish = result.finish;
   if (result.terminationReason !== "completed" || !finish?.metadata) {
     const errorDetails = result.errors?.map((e) => e.message).join("; ") || "unknown reason";
     console.error(`[warp_grep] processAgentResult failed. Reason: ${result.terminationReason}. Errors: ${errorDetails}. Turns: ${result.timings?.turns?.length ?? 0}`);
-    return { success: false, error: `Search did not complete: ${errorDetails}` };
+    return { success: false, error: `Search did not complete: ${errorDetails}`, ...timings ? { timings } : {} };
   }
   const contexts = (finish.resolved ?? []).map((r) => ({
     file: r.path,
@@ -2166,12 +2197,12 @@ async function processAgentResult(result, searchTerm, apiKey, apiUrl) {
   } catch (error) {
     console.error(`[warp_grep] Failed to compact result contexts:`, error);
   }
-  return { success: true, contexts, summary: finish.payload };
+  return { success: true, contexts, summary: finish.payload, ...timings ? { timings } : {} };
 }
 async function* executeToolCallStreaming(input, config) {
   const parsed = typeof input === "string" ? JSON.parse(input) : input;
   const allowNames = config.search_type === "node_modules" ? ["node_modules"] : void 0;
-  const provider = config.remoteCommands ? new RemoteCommandsProvider(config.repoRoot, config.remoteCommands) : config.provider ?? await getLocalProvider(config.repoRoot, config.excludes, allowNames);
+  const provider = config.remoteCommands ? new RemoteCommandsProvider(config.repoRoot, config.remoteCommands) : config.provider ?? await getLocalProvider(config.repoRoot, config.excludes, allowNames, config.includes);
   const generator = runWarpGrepStreaming({
     searchTerm: parsed.search_term,
     repoRoot: config.repoRoot,
@@ -2183,7 +2214,12 @@ async function* executeToolCallStreaming(input, config) {
     morphApiUrl: config.morphApiUrl,
     retryConfig: config.retryConfig,
     timeout: config.timeout,
-    search_type: config.search_type
+    search_type: config.search_type,
+    model: config.model,
+    temperature: config.temperature,
+    maxTokens: config.maxTokens,
+    maxTurns: config.maxTurns,
+    useBuiltinTools: config.useBuiltinTools
   });
   let agentResult;
   for (; ; ) {
diff --git a/dist/tools/warp_grep/gemini.cjs b/dist/tools/warp_grep/gemini.cjs
index cf4de9b624f299c6b1005740a147c15a7bbc4060..38d30e878c7582aa408eaf17b8ae4579408627b3 100644
--- a/dist/tools/warp_grep/gemini.cjs
+++ b/dist/tools/warp_grep/gemini.cjs
@@ -388,8 +388,12 @@ var init_local = __esm({
         if (options?.allowNames?.length) {
           this.allowNames = new Set(options.allowNames);
         }
+        if (options?.includes?.length) {
+          this.includes = [...options.includes];
+        }
       }
       allowNames;
+      includes;
       async grep(params) {
         let abs;
         try {
@@ -416,6 +420,7 @@ var init_local = __esm({
           contextLines,
           ...params.case_sensitive === false ? ["--ignore-case"] : [],
           ...params.glob ? ["--glob", params.glob] : [],
+          ...this.includes ? this.includes.flatMap((g) => ["-g", g]) : [],
           ...this.excludes.filter((e) => !this.allowNames?.has(e)).flatMap((e) => ["-g", `!${e}`]),
           params.pattern,
           targetArg || "."
@@ -591,6 +596,7 @@ Details: ${res.stderr}` : ""}`
           "--color=never",
           "-g",
           params.pattern,
+          ...this.includes ? this.includes.flatMap((g) => ["-g", g]) : [],
           ...this.excludes.filter((e) => !this.allowNames?.has(e)).flatMap((e) => ["-g", `!${e}`]),
           targetArg || "."
         ];
@@ -1358,6 +1364,9 @@ async function callModel(messages, model, options = {}) {
     defaultHeaders: { "X-Morph-SDK-Version": SDK_VERSION }
   });
   const MAX_EMPTY_RETRIES = 1;
+  const temperature = options.temperature ?? 0;
+  const maxTokens = options.maxTokens ?? 2048;
+  const useBuiltinTools = options.useBuiltinTools === true;
   logger.debug("WarpGrep", "model_call", { url: baseURL, model, message_count: messages.length });
   const callStartTime = Date.now();
   for (let attempt = 0; attempt <= MAX_EMPTY_RETRIES; attempt++) {
@@ -1365,10 +1374,10 @@ async function callModel(messages, model, options = {}) {
     try {
       data = await client.chat.completions.create({
         model,
-        temperature: 0,
-        max_tokens: 2048,
+        temperature,
+        max_tokens: maxTokens,
         messages,
-        tools: TOOL_SPECS,
+        ...useBuiltinTools ? {} : { tools: TOOL_SPECS },
         ...options.search_type ? { search_type: options.search_type } : {}
       });
     } catch (error) {
@@ -1469,8 +1478,11 @@ async function* runWarpGrepStreaming(config) {
   const timings = { turns: [], timeout_ms: timeoutMs };
   const repoRoot = config.repoRoot || process.cwd();
   const model = config.model || DEFAULT_MODEL;
+  const temperature = config.temperature;
+  const maxTokens = config.maxTokens;
+  const useBuiltinTools = config.useBuiltinTools === true;
   const messages = [];
-  const maxTurns = AGENT_CONFIG.MAX_TURNS;
+  const maxTurns = config.maxTurns ?? AGENT_CONFIG.MAX_TURNS;
   logger.debug("WarpGrep", "session_start", { search_term: config.searchTerm, model, max_turns: maxTurns });
   const initialStateStart = Date.now();
   const initialState = await buildInitialState(repoRoot, config.searchTerm, config.provider, { search_type: config.search_type });
@@ -1496,7 +1508,10 @@ async function* runWarpGrepStreaming(config) {
       morphApiUrl: config.morphApiUrl,
       retryConfig: config.retryConfig,
       timeout: timeoutMs,
-      search_type: config.search_type
+      search_type: config.search_type,
+      temperature,
+      maxTokens,
+      useBuiltinTools
     }).catch((e) => {
       const errMsg = e instanceof Error ? e.message : String(e);
       console.error(`[warp_grep] Morph API call failed on turn ${turn}:`, errMsg);
@@ -1871,12 +1886,12 @@ function createCodeStorageHttpCommands(config) {
 // tools/warp_grep/utils/github.ts
 function parseGitHubUrl(input) {
   input = input.trim();
-  const urlPattern = /^(?:https?:\/\/)?github\.com\/([^\/]+)\/([^\/\?\#]+)/;
+  const urlPattern = /^(?:https?:\/\/)?github\.com\/([^/]+)\/([^/?#]+)/;
   const match = input.match(urlPattern);
   if (match) {
     return { owner: match[1], repo: match[2].replace(/\.git$/, "") };
   }
-  const shortPattern = /^([^\/]+)\/([^\/]+)$/;
+  const shortPattern = /^([^/]+)\/([^/]+)$/;
   const shortMatch = input.match(shortPattern);
   if (shortMatch && !shortMatch[1].includes(".")) {
     return { owner: shortMatch[1], repo: shortMatch[2].replace(/\.git$/, "") };
@@ -2013,9 +2028,14 @@ async function compactFileContexts(files, query, apiKey, apiUrl, maxTokens = 1e4
 
 // tools/warp_grep/client.ts
 var DEFAULT_CODE_SEARCH_URL = "https://morphllm.com";
-async function getLocalProvider(repoRoot, excludes, allowNames) {
+async function getLocalProvider(repoRoot, excludes, allowNames, includes) {
   const { LocalRipgrepProvider: LocalRipgrepProvider2 } = await Promise.resolve().then(() => (init_local(), local_exports));
-  const opts = allowNames?.length ? { allowNames } : void 0;
+  const hasAllowNames = allowNames?.length;
+  const hasIncludes = includes?.length;
+  const opts = hasAllowNames || hasIncludes ? {
+    ...hasAllowNames ? { allowNames } : {},
+    ...hasIncludes ? { includes } : {}
+  } : void 0;
   return new LocalRipgrepProvider2(repoRoot, excludes, opts);
 }
 var WarpGrepClient = class {
@@ -2035,7 +2055,12 @@ var WarpGrepClient = class {
       morphApiUrl: this.config.morphApiUrl,
       retryConfig: this.config.retryConfig,
       timeout: this.config.timeout,
-      search_type: input.search_type
+      search_type: input.search_type,
+      model: input.model ?? this.config.model,
+      temperature: input.temperature ?? this.config.temperature,
+      maxTokens: input.maxTokens ?? this.config.maxTokens,
+      maxTurns: input.maxTurns ?? this.config.maxTurns,
+      useBuiltinTools: input.useBuiltinTools ?? this.config.useBuiltinTools
     };
     if (input.streamSteps) {
       return executeToolCallStreaming({ search_term: input.searchTerm }, toolConfig);
@@ -2123,7 +2148,7 @@ var WarpGrepClient = class {
 async function executeToolCall(input, config) {
   const parsed = typeof input === "string" ? JSON.parse(input) : input;
   const allowNames = config.search_type === "node_modules" ? ["node_modules"] : void 0;
-  const provider = config.remoteCommands ? new RemoteCommandsProvider(config.repoRoot, config.remoteCommands) : config.provider ?? await getLocalProvider(config.repoRoot, config.excludes, allowNames);
+  const provider = config.remoteCommands ? new RemoteCommandsProvider(config.repoRoot, config.remoteCommands) : config.provider ?? await getLocalProvider(config.repoRoot, config.excludes, allowNames, config.includes);
   const result = await runWarpGrep({
     searchTerm: parsed.search_term,
     repoRoot: config.repoRoot,
@@ -2135,16 +2160,22 @@ async function executeToolCall(input, config) {
     morphApiUrl: config.morphApiUrl,
     retryConfig: config.retryConfig,
     timeout: config.timeout,
-    search_type: config.search_type
+    search_type: config.search_type,
+    model: config.model,
+    temperature: config.temperature,
+    maxTokens: config.maxTokens,
+    maxTurns: config.maxTurns,
+    useBuiltinTools: config.useBuiltinTools
   });
   return processAgentResult(result, parsed.search_term, config.morphApiKey, config.morphApiUrl);
 }
 async function processAgentResult(result, searchTerm, apiKey, apiUrl) {
+  const timings = result.timings;
   const finish = result.finish;
   if (result.terminationReason !== "completed" || !finish?.metadata) {
     const errorDetails = result.errors?.map((e) => e.message).join("; ") || "unknown reason";
     console.error(`[warp_grep] processAgentResult failed. Reason: ${result.terminationReason}. Errors: ${errorDetails}. Turns: ${result.timings?.turns?.length ?? 0}`);
-    return { success: false, error: `Search did not complete: ${errorDetails}` };
+    return { success: false, error: `Search did not complete: ${errorDetails}`, ...timings ? { timings } : {} };
   }
   const contexts = (finish.resolved ?? []).map((r) => ({
     file: r.path,
@@ -2160,12 +2191,12 @@ async function processAgentResult(result, searchTerm, apiKey, apiUrl) {
   } catch (error) {
     console.error(`[warp_grep] Failed to compact result contexts:`, error);
   }
-  return { success: true, contexts, summary: finish.payload };
+  return { success: true, contexts, summary: finish.payload, ...timings ? { timings } : {} };
 }
 async function* executeToolCallStreaming(input, config) {
   const parsed = typeof input === "string" ? JSON.parse(input) : input;
   const allowNames = config.search_type === "node_modules" ? ["node_modules"] : void 0;
-  const provider = config.remoteCommands ? new RemoteCommandsProvider(config.repoRoot, config.remoteCommands) : config.provider ?? await getLocalProvider(config.repoRoot, config.excludes, allowNames);
+  const provider = config.remoteCommands ? new RemoteCommandsProvider(config.repoRoot, config.remoteCommands) : config.provider ?? await getLocalProvider(config.repoRoot, config.excludes, allowNames, config.includes);
   const generator = runWarpGrepStreaming({
     searchTerm: parsed.search_term,
     repoRoot: config.repoRoot,
@@ -2177,7 +2208,12 @@ async function* executeToolCallStreaming(input, config) {
     morphApiUrl: config.morphApiUrl,
     retryConfig: config.retryConfig,
     timeout: config.timeout,
-    search_type: config.search_type
+    search_type: config.search_type,
+    model: config.model,
+    temperature: config.temperature,
+    maxTokens: config.maxTokens,
+    maxTurns: config.maxTurns,
+    useBuiltinTools: config.useBuiltinTools
   });
   let agentResult;
   for (; ; ) {
diff --git a/dist/tools/warp_grep/index.cjs b/dist/tools/warp_grep/index.cjs
index beaddc85433468d4ece48f734ee2419ef0de7bbd..dbcaede1e4edc09fdf2ae22c0161c43ebfb2e25e 100644
--- a/dist/tools/warp_grep/index.cjs
+++ b/dist/tools/warp_grep/index.cjs
@@ -388,8 +388,12 @@ var init_local = __esm({
         if (options?.allowNames?.length) {
           this.allowNames = new Set(options.allowNames);
         }
+        if (options?.includes?.length) {
+          this.includes = [...options.includes];
+        }
       }
       allowNames;
+      includes;
       async grep(params) {
         let abs;
         try {
@@ -416,6 +420,7 @@ var init_local = __esm({
           contextLines,
           ...params.case_sensitive === false ? ["--ignore-case"] : [],
           ...params.glob ? ["--glob", params.glob] : [],
+          ...this.includes ? this.includes.flatMap((g) => ["-g", g]) : [],
           ...this.excludes.filter((e) => !this.allowNames?.has(e)).flatMap((e) => ["-g", `!${e}`]),
           params.pattern,
           targetArg || "."
@@ -591,6 +596,7 @@ Details: ${res.stderr}` : ""}`
           "--color=never",
           "-g",
           params.pattern,
+          ...this.includes ? this.includes.flatMap((g) => ["-g", g]) : [],
           ...this.excludes.filter((e) => !this.allowNames?.has(e)).flatMap((e) => ["-g", `!${e}`]),
           targetArg || "."
         ];
@@ -1385,6 +1391,9 @@ async function callModel(messages, model, options = {}) {
     defaultHeaders: { "X-Morph-SDK-Version": SDK_VERSION }
   });
   const MAX_EMPTY_RETRIES = 1;
+  const temperature = options.temperature ?? 0;
+  const maxTokens = options.maxTokens ?? 2048;
+  const useBuiltinTools = options.useBuiltinTools === true;
   logger.debug("WarpGrep", "model_call", { url: baseURL, model, message_count: messages.length });
   const callStartTime = Date.now();
   for (let attempt = 0; attempt <= MAX_EMPTY_RETRIES; attempt++) {
@@ -1392,10 +1401,10 @@ async function callModel(messages, model, options = {}) {
     try {
       data = await client.chat.completions.create({
         model,
-        temperature: 0,
-        max_tokens: 2048,
+        temperature,
+        max_tokens: maxTokens,
         messages,
-        tools: TOOL_SPECS,
+        ...useBuiltinTools ? {} : { tools: TOOL_SPECS },
         ...options.search_type ? { search_type: options.search_type } : {}
       });
     } catch (error) {
@@ -1496,8 +1505,11 @@ async function* runWarpGrepStreaming(config) {
   const timings = { turns: [], timeout_ms: timeoutMs };
   const repoRoot = config.repoRoot || process.cwd();
   const model = config.model || DEFAULT_MODEL;
+  const temperature = config.temperature;
+  const maxTokens = config.maxTokens;
+  const useBuiltinTools = config.useBuiltinTools === true;
   const messages = [];
-  const maxTurns = AGENT_CONFIG.MAX_TURNS;
+  const maxTurns = config.maxTurns ?? AGENT_CONFIG.MAX_TURNS;
   logger.debug("WarpGrep", "session_start", { search_term: config.searchTerm, model, max_turns: maxTurns });
   const initialStateStart = Date.now();
   const initialState = await buildInitialState(repoRoot, config.searchTerm, config.provider, { search_type: config.search_type });
@@ -1523,7 +1535,10 @@ async function* runWarpGrepStreaming(config) {
       morphApiUrl: config.morphApiUrl,
       retryConfig: config.retryConfig,
       timeout: timeoutMs,
-      search_type: config.search_type
+      search_type: config.search_type,
+      temperature,
+      maxTokens,
+      useBuiltinTools
     }).catch((e) => {
       const errMsg = e instanceof Error ? e.message : String(e);
       console.error(`[warp_grep] Morph API call failed on turn ${turn}:`, errMsg);
@@ -1898,12 +1913,12 @@ function createCodeStorageHttpCommands(config) {
 // tools/warp_grep/utils/github.ts
 function parseGitHubUrl(input) {
   input = input.trim();
-  const urlPattern = /^(?:https?:\/\/)?github\.com\/([^\/]+)\/([^\/\?\#]+)/;
+  const urlPattern = /^(?:https?:\/\/)?github\.com\/([^/]+)\/([^/?#]+)/;
   const match = input.match(urlPattern);
   if (match) {
     return { owner: match[1], repo: match[2].replace(/\.git$/, "") };
   }
-  const shortPattern = /^([^\/]+)\/([^\/]+)$/;
+  const shortPattern = /^([^/]+)\/([^/]+)$/;
   const shortMatch = input.match(shortPattern);
   if (shortMatch && !shortMatch[1].includes(".")) {
     return { owner: shortMatch[1], repo: shortMatch[2].replace(/\.git$/, "") };
@@ -2040,9 +2055,14 @@ async function compactFileContexts(files, query, apiKey, apiUrl, maxTokens = 1e4
 
 // tools/warp_grep/client.ts
 var DEFAULT_CODE_SEARCH_URL = "https://morphllm.com";
-async function getLocalProvider(repoRoot, excludes, allowNames) {
+async function getLocalProvider(repoRoot, excludes, allowNames, includes) {
   const { LocalRipgrepProvider: LocalRipgrepProvider2 } = await Promise.resolve().then(() => (init_local(), local_exports));
-  const opts = allowNames?.length ? { allowNames } : void 0;
+  const hasAllowNames = allowNames?.length;
+  const hasIncludes = includes?.length;
+  const opts = hasAllowNames || hasIncludes ? {
+    ...hasAllowNames ? { allowNames } : {},
+    ...hasIncludes ? { includes } : {}
+  } : void 0;
   return new LocalRipgrepProvider2(repoRoot, excludes, opts);
 }
 var WarpGrepClient = class {
@@ -2062,7 +2082,12 @@ var WarpGrepClient = class {
       morphApiUrl: this.config.morphApiUrl,
       retryConfig: this.config.retryConfig,
       timeout: this.config.timeout,
-      search_type: input.search_type
+      search_type: input.search_type,
+      model: input.model ?? this.config.model,
+      temperature: input.temperature ?? this.config.temperature,
+      maxTokens: input.maxTokens ?? this.config.maxTokens,
+      maxTurns: input.maxTurns ?? this.config.maxTurns,
+      useBuiltinTools: input.useBuiltinTools ?? this.config.useBuiltinTools
     };
     if (input.streamSteps) {
       return executeToolCallStreaming({ search_term: input.searchTerm }, toolConfig);
@@ -2157,7 +2182,7 @@ function executeWarpGrep(input, config) {
 async function executeToolCall(input, config) {
   const parsed = typeof input === "string" ? JSON.parse(input) : input;
   const allowNames = config.search_type === "node_modules" ? ["node_modules"] : void 0;
-  const provider = config.remoteCommands ? new RemoteCommandsProvider(config.repoRoot, config.remoteCommands) : config.provider ?? await getLocalProvider(config.repoRoot, config.excludes, allowNames);
+  const provider = config.remoteCommands ? new RemoteCommandsProvider(config.repoRoot, config.remoteCommands) : config.provider ?? await getLocalProvider(config.repoRoot, config.excludes, allowNames, config.includes);
   const result = await runWarpGrep({
     searchTerm: parsed.search_term,
     repoRoot: config.repoRoot,
@@ -2169,16 +2194,22 @@ async function executeToolCall(input, config) {
     morphApiUrl: config.morphApiUrl,
     retryConfig: config.retryConfig,
     timeout: config.timeout,
-    search_type: config.search_type
+    search_type: config.search_type,
+    model: config.model,
+    temperature: config.temperature,
+    maxTokens: config.maxTokens,
+    maxTurns: config.maxTurns,
+    useBuiltinTools: config.useBuiltinTools
   });
   return processAgentResult(result, parsed.search_term, config.morphApiKey, config.morphApiUrl);
 }
 async function processAgentResult(result, searchTerm, apiKey, apiUrl) {
+  const timings = result.timings;
   const finish = result.finish;
   if (result.terminationReason !== "completed" || !finish?.metadata) {
     const errorDetails = result.errors?.map((e) => e.message).join("; ") || "unknown reason";
     console.error(`[warp_grep] processAgentResult failed. Reason: ${result.terminationReason}. Errors: ${errorDetails}. Turns: ${result.timings?.turns?.length ?? 0}`);
-    return { success: false, error: `Search did not complete: ${errorDetails}` };
+    return { success: false, error: `Search did not complete: ${errorDetails}`, ...timings ? { timings } : {} };
   }
   const contexts = (finish.resolved ?? []).map((r) => ({
     file: r.path,
@@ -2194,12 +2225,12 @@ async function processAgentResult(result, searchTerm, apiKey, apiUrl) {
   } catch (error) {
     console.error(`[warp_grep] Failed to compact result contexts:`, error);
   }
-  return { success: true, contexts, summary: finish.payload };
+  return { success: true, contexts, summary: finish.payload, ...timings ? { timings } : {} };
 }
 async function* executeToolCallStreaming(input, config) {
   const parsed = typeof input === "string" ? JSON.parse(input) : input;
   const allowNames = config.search_type === "node_modules" ? ["node_modules"] : void 0;
-  const provider = config.remoteCommands ? new RemoteCommandsProvider(config.repoRoot, config.remoteCommands) : config.provider ?? await getLocalProvider(config.repoRoot, config.excludes, allowNames);
+  const provider = config.remoteCommands ? new RemoteCommandsProvider(config.repoRoot, config.remoteCommands) : config.provider ?? await getLocalProvider(config.repoRoot, config.excludes, allowNames, config.includes);
   const generator = runWarpGrepStreaming({
     searchTerm: parsed.search_term,
     repoRoot: config.repoRoot,
@@ -2211,7 +2242,12 @@ async function* executeToolCallStreaming(input, config) {
     morphApiUrl: config.morphApiUrl,
     retryConfig: config.retryConfig,
     timeout: config.timeout,
-    search_type: config.search_type
+    search_type: config.search_type,
+    model: config.model,
+    temperature: config.temperature,
+    maxTokens: config.maxTokens,
+    maxTurns: config.maxTurns,
+    useBuiltinTools: config.useBuiltinTools
   });
   let agentResult;
   for (; ; ) {
diff --git a/dist/tools/warp_grep/index.d.ts b/dist/tools/warp_grep/index.d.ts
index 37ee1434c74f5bf693cf53b55e935c1e8fff1ec3..5e8a69d685c462fdb5c5c1f6999b4528eb6343f3 100644
--- a/dist/tools/warp_grep/index.d.ts
+++ b/dist/tools/warp_grep/index.d.ts
@@ -1,4 +1,4 @@
-import { z } from 'zod';
+import * as z from 'zod';
 export { WarpGrepClient, executeGitHubReadFile, executeToolCall, executeWarpGrep, formatGitHubReadFileResult, formatResult } from './client.js';
 export { e as GitHubReadFileInput, f as GitHubReadFileResult, h as GitHubReadFileToolConfig, G as GitHubSearchInput, g as GitHubSearchToolConfig, R as RemoteCommands, W as WarpGrepClientConfig, c as WarpGrepContext, a as WarpGrepInput, b as WarpGrepResult, d as WarpGrepToolConfig } from '../../types-CtkD4o1_.js';
 export { GrepResult, ListDirectoryEntry, ReadResult, WarpGrepProvider } from './providers/types.js';
diff --git a/dist/tools/warp_grep/openai.cjs b/dist/tools/warp_grep/openai.cjs
index 36befdc6952b3dd94bdc96fc41fa9ebcddb391cc..e6c44a9acb3095f5dd939f64792e35feb5282fec 100644
--- a/dist/tools/warp_grep/openai.cjs
+++ b/dist/tools/warp_grep/openai.cjs
@@ -388,8 +388,12 @@ var init_local = __esm({
         if (options?.allowNames?.length) {
           this.allowNames = new Set(options.allowNames);
         }
+        if (options?.includes?.length) {
+          this.includes = [...options.includes];
+        }
       }
       allowNames;
+      includes;
       async grep(params) {
         let abs;
         try {
@@ -416,6 +420,7 @@ var init_local = __esm({
           contextLines,
           ...params.case_sensitive === false ? ["--ignore-case"] : [],
           ...params.glob ? ["--glob", params.glob] : [],
+          ...this.includes ? this.includes.flatMap((g) => ["-g", g]) : [],
           ...this.excludes.filter((e) => !this.allowNames?.has(e)).flatMap((e) => ["-g", `!${e}`]),
           params.pattern,
           targetArg || "."
@@ -591,6 +596,7 @@ Details: ${res.stderr}` : ""}`
           "--color=never",
           "-g",
           params.pattern,
+          ...this.includes ? this.includes.flatMap((g) => ["-g", g]) : [],
           ...this.excludes.filter((e) => !this.allowNames?.has(e)).flatMap((e) => ["-g", `!${e}`]),
           targetArg || "."
         ];
@@ -1357,6 +1363,9 @@ async function callModel(messages, model, options = {}) {
     defaultHeaders: { "X-Morph-SDK-Version": SDK_VERSION }
   });
   const MAX_EMPTY_RETRIES = 1;
+  const temperature = options.temperature ?? 0;
+  const maxTokens = options.maxTokens ?? 2048;
+  const useBuiltinTools = options.useBuiltinTools === true;
   logger.debug("WarpGrep", "model_call", { url: baseURL, model, message_count: messages.length });
   const callStartTime = Date.now();
   for (let attempt = 0; attempt <= MAX_EMPTY_RETRIES; attempt++) {
@@ -1364,10 +1373,10 @@ async function callModel(messages, model, options = {}) {
     try {
       data = await client.chat.completions.create({
         model,
-        temperature: 0,
-        max_tokens: 2048,
+        temperature,
+        max_tokens: maxTokens,
         messages,
-        tools: TOOL_SPECS,
+        ...useBuiltinTools ? {} : { tools: TOOL_SPECS },
         ...options.search_type ? { search_type: options.search_type } : {}
       });
     } catch (error) {
@@ -1468,8 +1477,11 @@ async function* runWarpGrepStreaming(config) {
   const timings = { turns: [], timeout_ms: timeoutMs };
   const repoRoot = config.repoRoot || process.cwd();
   const model = config.model || DEFAULT_MODEL;
+  const temperature = config.temperature;
+  const maxTokens = config.maxTokens;
+  const useBuiltinTools = config.useBuiltinTools === true;
   const messages = [];
-  const maxTurns = AGENT_CONFIG.MAX_TURNS;
+  const maxTurns = config.maxTurns ?? AGENT_CONFIG.MAX_TURNS;
   logger.debug("WarpGrep", "session_start", { search_term: config.searchTerm, model, max_turns: maxTurns });
   const initialStateStart = Date.now();
   const initialState = await buildInitialState(repoRoot, config.searchTerm, config.provider, { search_type: config.search_type });
@@ -1495,7 +1507,10 @@ async function* runWarpGrepStreaming(config) {
       morphApiUrl: config.morphApiUrl,
       retryConfig: config.retryConfig,
       timeout: timeoutMs,
-      search_type: config.search_type
+      search_type: config.search_type,
+      temperature,
+      maxTokens,
+      useBuiltinTools
     }).catch((e) => {
       const errMsg = e instanceof Error ? e.message : String(e);
       console.error(`[warp_grep] Morph API call failed on turn ${turn}:`, errMsg);
@@ -1870,12 +1885,12 @@ function createCodeStorageHttpCommands(config) {
 // tools/warp_grep/utils/github.ts
 function parseGitHubUrl(input) {
   input = input.trim();
-  const urlPattern = /^(?:https?:\/\/)?github\.com\/([^\/]+)\/([^\/\?\#]+)/;
+  const urlPattern = /^(?:https?:\/\/)?github\.com\/([^/]+)\/([^/?#]+)/;
   const match = input.match(urlPattern);
   if (match) {
     return { owner: match[1], repo: match[2].replace(/\.git$/, "") };
   }
-  const shortPattern = /^([^\/]+)\/([^\/]+)$/;
+  const shortPattern = /^([^/]+)\/([^/]+)$/;
   const shortMatch = input.match(shortPattern);
   if (shortMatch && !shortMatch[1].includes(".")) {
     return { owner: shortMatch[1], repo: shortMatch[2].replace(/\.git$/, "") };
@@ -2012,9 +2027,14 @@ async function compactFileContexts(files, query, apiKey, apiUrl, maxTokens = 1e4
 
 // tools/warp_grep/client.ts
 var DEFAULT_CODE_SEARCH_URL = "https://morphllm.com";
-async function getLocalProvider(repoRoot, excludes, allowNames) {
+async function getLocalProvider(repoRoot, excludes, allowNames, includes) {
   const { LocalRipgrepProvider: LocalRipgrepProvider2 } = await Promise.resolve().then(() => (init_local(), local_exports));
-  const opts = allowNames?.length ? { allowNames } : void 0;
+  const hasAllowNames = allowNames?.length;
+  const hasIncludes = includes?.length;
+  const opts = hasAllowNames || hasIncludes ? {
+    ...hasAllowNames ? { allowNames } : {},
+    ...hasIncludes ? { includes } : {}
+  } : void 0;
   return new LocalRipgrepProvider2(repoRoot, excludes, opts);
 }
 var WarpGrepClient = class {
@@ -2034,7 +2054,12 @@ var WarpGrepClient = class {
       morphApiUrl: this.config.morphApiUrl,
       retryConfig: this.config.retryConfig,
       timeout: this.config.timeout,
-      search_type: input.search_type
+      search_type: input.search_type,
+      model: input.model ?? this.config.model,
+      temperature: input.temperature ?? this.config.temperature,
+      maxTokens: input.maxTokens ?? this.config.maxTokens,
+      maxTurns: input.maxTurns ?? this.config.maxTurns,
+      useBuiltinTools: input.useBuiltinTools ?? this.config.useBuiltinTools
     };
     if (input.streamSteps) {
       return executeToolCallStreaming({ search_term: input.searchTerm }, toolConfig);
@@ -2122,7 +2147,7 @@ var WarpGrepClient = class {
 async function executeToolCall(input, config) {
   const parsed = typeof input === "string" ? JSON.parse(input) : input;
   const allowNames = config.search_type === "node_modules" ? ["node_modules"] : void 0;
-  const provider = config.remoteCommands ? new RemoteCommandsProvider(config.repoRoot, config.remoteCommands) : config.provider ?? await getLocalProvider(config.repoRoot, config.excludes, allowNames);
+  const provider = config.remoteCommands ? new RemoteCommandsProvider(config.repoRoot, config.remoteCommands) : config.provider ?? await getLocalProvider(config.repoRoot, config.excludes, allowNames, config.includes);
   const result = await runWarpGrep({
     searchTerm: parsed.search_term,
     repoRoot: config.repoRoot,
@@ -2134,16 +2159,22 @@ async function executeToolCall(input, config) {
     morphApiUrl: config.morphApiUrl,
     retryConfig: config.retryConfig,
     timeout: config.timeout,
-    search_type: config.search_type
+    search_type: config.search_type,
+    model: config.model,
+    temperature: config.temperature,
+    maxTokens: config.maxTokens,
+    maxTurns: config.maxTurns,
+    useBuiltinTools: config.useBuiltinTools
   });
   return processAgentResult(result, parsed.search_term, config.morphApiKey, config.morphApiUrl);
 }
 async function processAgentResult(result, searchTerm, apiKey, apiUrl) {
+  const timings = result.timings;
   const finish = result.finish;
   if (result.terminationReason !== "completed" || !finish?.metadata) {
     const errorDetails = result.errors?.map((e) => e.message).join("; ") || "unknown reason";
     console.error(`[warp_grep] processAgentResult failed. Reason: ${result.terminationReason}. Errors: ${errorDetails}. Turns: ${result.timings?.turns?.length ?? 0}`);
-    return { success: false, error: `Search did not complete: ${errorDetails}` };
+    return { success: false, error: `Search did not complete: ${errorDetails}`, ...timings ? { timings } : {} };
   }
   const contexts = (finish.resolved ?? []).map((r) => ({
     file: r.path,
@@ -2159,12 +2190,12 @@ async function processAgentResult(result, searchTerm, apiKey, apiUrl) {
   } catch (error) {
     console.error(`[warp_grep] Failed to compact result contexts:`, error);
   }
-  return { success: true, contexts, summary: finish.payload };
+  return { success: true, contexts, summary: finish.payload, ...timings ? { timings } : {} };
 }
 async function* executeToolCallStreaming(input, config) {
   const parsed = typeof input === "string" ? JSON.parse(input) : input;
   const allowNames = config.search_type === "node_modules" ? ["node_modules"] : void 0;
-  const provider = config.remoteCommands ? new RemoteCommandsProvider(config.repoRoot, config.remoteCommands) : config.provider ?? await getLocalProvider(config.repoRoot, config.excludes, allowNames);
+  const provider = config.remoteCommands ? new RemoteCommandsProvider(config.repoRoot, config.remoteCommands) : config.provider ?? await getLocalProvider(config.repoRoot, config.excludes, allowNames, config.includes);
   const generator = runWarpGrepStreaming({
     searchTerm: parsed.search_term,
     repoRoot: config.repoRoot,
@@ -2176,7 +2207,12 @@ async function* executeToolCallStreaming(input, config) {
     morphApiUrl: config.morphApiUrl,
     retryConfig: config.retryConfig,
     timeout: config.timeout,
-    search_type: config.search_type
+    search_type: config.search_type,
+    model: config.model,
+    temperature: config.temperature,
+    maxTokens: config.maxTokens,
+    maxTurns: config.maxTurns,
+    useBuiltinTools: config.useBuiltinTools
   });
   let agentResult;
   for (; ; ) {
diff --git a/dist/tools/warp_grep/providers/local.d.ts b/dist/tools/warp_grep/providers/local.d.ts
index 32d9ed065a91010c200fbdaacd729b3fd6b8177d..ab61d348ce273fcb1b0d73176458a310fd61173b 100644
--- a/dist/tools/warp_grep/providers/local.d.ts
+++ b/dist/tools/warp_grep/providers/local.d.ts
@@ -4,8 +4,10 @@ declare class LocalRipgrepProvider implements WarpGrepProvider {
     private readonly repoRoot;
     private readonly excludes;
     private readonly allowNames?;
+    private readonly includes?;
     constructor(repoRoot: string, excludes?: string[], options?: {
         allowNames?: string[];
+        includes?: string[];
     });
     grep(params: {
         pattern: string;
diff --git a/dist/tools/warp_grep/utils/github.cjs b/dist/tools/warp_grep/utils/github.cjs
index 0ecbc3c1668b6819b2d4304a116a9fdc7860255b..44967aacdf24bff88fe715780f22b6c12a206314 100644
--- a/dist/tools/warp_grep/utils/github.cjs
+++ b/dist/tools/warp_grep/utils/github.cjs
@@ -25,12 +25,12 @@ __export(github_exports, {
 module.exports = __toCommonJS(github_exports);
 function parseGitHubUrl(input) {
   input = input.trim();
-  const urlPattern = /^(?:https?:\/\/)?github\.com\/([^\/]+)\/([^\/\?\#]+)/;
+  const urlPattern = /^(?:https?:\/\/)?github\.com\/([^/]+)\/([^/?#]+)/;
   const match = input.match(urlPattern);
   if (match) {
     return { owner: match[1], repo: match[2].replace(/\.git$/, "") };
   }
-  const shortPattern = /^([^\/]+)\/([^\/]+)$/;
+  const shortPattern = /^([^/]+)\/([^/]+)$/;
   const shortMatch = input.match(shortPattern);
   if (shortMatch && !shortMatch[1].includes(".")) {
     return { owner: shortMatch[1], repo: shortMatch[2].replace(/\.git$/, "") };
diff --git a/dist/tools/warp_grep/vercel.cjs b/dist/tools/warp_grep/vercel.cjs
index 03106eb21fab813eb64bc13dc94df6b551c27c42..e292739c46729cad5862fedf8868273e56685b81 100644
--- a/dist/tools/warp_grep/vercel.cjs
+++ b/dist/tools/warp_grep/vercel.cjs
@@ -388,8 +388,12 @@ var init_local = __esm({
         if (options?.allowNames?.length) {
           this.allowNames = new Set(options.allowNames);
         }
+        if (options?.includes?.length) {
+          this.includes = [...options.includes];
+        }
       }
       allowNames;
+      includes;
       async grep(params) {
         let abs;
         try {
@@ -416,6 +420,7 @@ var init_local = __esm({
           contextLines,
           ...params.case_sensitive === false ? ["--ignore-case"] : [],
           ...params.glob ? ["--glob", params.glob] : [],
+          ...this.includes ? this.includes.flatMap((g) => ["-g", g]) : [],
           ...this.excludes.filter((e) => !this.allowNames?.has(e)).flatMap((e) => ["-g", `!${e}`]),
           params.pattern,
           targetArg || "."
@@ -591,6 +596,7 @@ Details: ${res.stderr}` : ""}`
           "--color=never",
           "-g",
           params.pattern,
+          ...this.includes ? this.includes.flatMap((g) => ["-g", g]) : [],
           ...this.excludes.filter((e) => !this.allowNames?.has(e)).flatMap((e) => ["-g", `!${e}`]),
           targetArg || "."
         ];
@@ -1359,6 +1365,9 @@ async function callModel(messages, model, options = {}) {
     defaultHeaders: { "X-Morph-SDK-Version": SDK_VERSION }
   });
   const MAX_EMPTY_RETRIES = 1;
+  const temperature = options.temperature ?? 0;
+  const maxTokens = options.maxTokens ?? 2048;
+  const useBuiltinTools = options.useBuiltinTools === true;
   logger.debug("WarpGrep", "model_call", { url: baseURL, model, message_count: messages.length });
   const callStartTime = Date.now();
   for (let attempt = 0; attempt <= MAX_EMPTY_RETRIES; attempt++) {
@@ -1366,10 +1375,10 @@ async function callModel(messages, model, options = {}) {
     try {
       data = await client.chat.completions.create({
         model,
-        temperature: 0,
-        max_tokens: 2048,
+        temperature,
+        max_tokens: maxTokens,
         messages,
-        tools: TOOL_SPECS,
+        ...useBuiltinTools ? {} : { tools: TOOL_SPECS },
         ...options.search_type ? { search_type: options.search_type } : {}
       });
     } catch (error) {
@@ -1470,8 +1479,11 @@ async function* runWarpGrepStreaming(config) {
   const timings = { turns: [], timeout_ms: timeoutMs };
   const repoRoot = config.repoRoot || process.cwd();
   const model = config.model || DEFAULT_MODEL;
+  const temperature = config.temperature;
+  const maxTokens = config.maxTokens;
+  const useBuiltinTools = config.useBuiltinTools === true;
   const messages = [];
-  const maxTurns = AGENT_CONFIG.MAX_TURNS;
+  const maxTurns = config.maxTurns ?? AGENT_CONFIG.MAX_TURNS;
   logger.debug("WarpGrep", "session_start", { search_term: config.searchTerm, model, max_turns: maxTurns });
   const initialStateStart = Date.now();
   const initialState = await buildInitialState(repoRoot, config.searchTerm, config.provider, { search_type: config.search_type });
@@ -1497,7 +1509,10 @@ async function* runWarpGrepStreaming(config) {
       morphApiUrl: config.morphApiUrl,
       retryConfig: config.retryConfig,
       timeout: timeoutMs,
-      search_type: config.search_type
+      search_type: config.search_type,
+      temperature,
+      maxTokens,
+      useBuiltinTools
     }).catch((e) => {
       const errMsg = e instanceof Error ? e.message : String(e);
       console.error(`[warp_grep] Morph API call failed on turn ${turn}:`, errMsg);
@@ -1872,12 +1887,12 @@ function createCodeStorageHttpCommands(config) {
 // tools/warp_grep/utils/github.ts
 function parseGitHubUrl(input) {
   input = input.trim();
-  const urlPattern = /^(?:https?:\/\/)?github\.com\/([^\/]+)\/([^\/\?\#]+)/;
+  const urlPattern = /^(?:https?:\/\/)?github\.com\/([^/]+)\/([^/?#]+)/;
   const match = input.match(urlPattern);
   if (match) {
     return { owner: match[1], repo: match[2].replace(/\.git$/, "") };
   }
-  const shortPattern = /^([^\/]+)\/([^\/]+)$/;
+  const shortPattern = /^([^/]+)\/([^/]+)$/;
   const shortMatch = input.match(shortPattern);
   if (shortMatch && !shortMatch[1].includes(".")) {
     return { owner: shortMatch[1], repo: shortMatch[2].replace(/\.git$/, "") };
@@ -2014,9 +2029,14 @@ async function compactFileContexts(files, query, apiKey, apiUrl, maxTokens = 1e4
 
 // tools/warp_grep/client.ts
 var DEFAULT_CODE_SEARCH_URL = "https://morphllm.com";
-async function getLocalProvider(repoRoot, excludes, allowNames) {
+async function getLocalProvider(repoRoot, excludes, allowNames, includes) {
   const { LocalRipgrepProvider: LocalRipgrepProvider2 } = await Promise.resolve().then(() => (init_local(), local_exports));
-  const opts = allowNames?.length ? { allowNames } : void 0;
+  const hasAllowNames = allowNames?.length;
+  const hasIncludes = includes?.length;
+  const opts = hasAllowNames || hasIncludes ? {
+    ...hasAllowNames ? { allowNames } : {},
+    ...hasIncludes ? { includes } : {}
+  } : void 0;
   return new LocalRipgrepProvider2(repoRoot, excludes, opts);
 }
 var WarpGrepClient = class {
@@ -2036,7 +2056,12 @@ var WarpGrepClient = class {
       morphApiUrl: this.config.morphApiUrl,
       retryConfig: this.config.retryConfig,
       timeout: this.config.timeout,
-      search_type: input.search_type
+      search_type: input.search_type,
+      model: input.model ?? this.config.model,
+      temperature: input.temperature ?? this.config.temperature,
+      maxTokens: input.maxTokens ?? this.config.maxTokens,
+      maxTurns: input.maxTurns ?? this.config.maxTurns,
+      useBuiltinTools: input.useBuiltinTools ?? this.config.useBuiltinTools
     };
     if (input.streamSteps) {
       return executeToolCallStreaming({ search_term: input.searchTerm }, toolConfig);
@@ -2124,7 +2149,7 @@ var WarpGrepClient = class {
 async function executeToolCall(input, config) {
   const parsed = typeof input === "string" ? JSON.parse(input) : input;
   const allowNames = config.search_type === "node_modules" ? ["node_modules"] : void 0;
-  const provider = config.remoteCommands ? new RemoteCommandsProvider(config.repoRoot, config.remoteCommands) : config.provider ?? await getLocalProvider(config.repoRoot, config.excludes, allowNames);
+  const provider = config.remoteCommands ? new RemoteCommandsProvider(config.repoRoot, config.remoteCommands) : config.provider ?? await getLocalProvider(config.repoRoot, config.excludes, allowNames, config.includes);
   const result = await runWarpGrep({
     searchTerm: parsed.search_term,
     repoRoot: config.repoRoot,
@@ -2136,16 +2161,22 @@ async function executeToolCall(input, config) {
     morphApiUrl: config.morphApiUrl,
     retryConfig: config.retryConfig,
     timeout: config.timeout,
-    search_type: config.search_type
+    search_type: config.search_type,
+    model: config.model,
+    temperature: config.temperature,
+    maxTokens: config.maxTokens,
+    maxTurns: config.maxTurns,
+    useBuiltinTools: config.useBuiltinTools
   });
   return processAgentResult(result, parsed.search_term, config.morphApiKey, config.morphApiUrl);
 }
 async function processAgentResult(result, searchTerm, apiKey, apiUrl) {
+  const timings = result.timings;
   const finish = result.finish;
   if (result.terminationReason !== "completed" || !finish?.metadata) {
     const errorDetails = result.errors?.map((e) => e.message).join("; ") || "unknown reason";
     console.error(`[warp_grep] processAgentResult failed. Reason: ${result.terminationReason}. Errors: ${errorDetails}. Turns: ${result.timings?.turns?.length ?? 0}`);
-    return { success: false, error: `Search did not complete: ${errorDetails}` };
+    return { success: false, error: `Search did not complete: ${errorDetails}`, ...timings ? { timings } : {} };
   }
   const contexts = (finish.resolved ?? []).map((r) => ({
     file: r.path,
@@ -2161,12 +2192,12 @@ async function processAgentResult(result, searchTerm, apiKey, apiUrl) {
   } catch (error) {
     console.error(`[warp_grep] Failed to compact result contexts:`, error);
   }
-  return { success: true, contexts, summary: finish.payload };
+  return { success: true, contexts, summary: finish.payload, ...timings ? { timings } : {} };
 }
 async function* executeToolCallStreaming(input, config) {
   const parsed = typeof input === "string" ? JSON.parse(input) : input;
   const allowNames = config.search_type === "node_modules" ? ["node_modules"] : void 0;
-  const provider = config.remoteCommands ? new RemoteCommandsProvider(config.repoRoot, config.remoteCommands) : config.provider ?? await getLocalProvider(config.repoRoot, config.excludes, allowNames);
+  const provider = config.remoteCommands ? new RemoteCommandsProvider(config.repoRoot, config.remoteCommands) : config.provider ?? await getLocalProvider(config.repoRoot, config.excludes, allowNames, config.includes);
   const generator = runWarpGrepStreaming({
     searchTerm: parsed.search_term,
     repoRoot: config.repoRoot,
@@ -2178,7 +2209,12 @@ async function* executeToolCallStreaming(input, config) {
     morphApiUrl: config.morphApiUrl,
     retryConfig: config.retryConfig,
     timeout: config.timeout,
-    search_type: config.search_type
+    search_type: config.search_type,
+    model: config.model,
+    temperature: config.temperature,
+    maxTokens: config.maxTokens,
+    maxTurns: config.maxTurns,
+    useBuiltinTools: config.useBuiltinTools
   });
   let agentResult;
   for (; ; ) {
diff --git a/dist/types-CtkD4o1_.d.ts b/dist/types-CtkD4o1_.d.ts
index 7af07831f5f717dc873e826d43a3672b6f312adb..6aac8202c1755be8785a03c329d6c73b4e611f97 100644
--- a/dist/types-CtkD4o1_.d.ts
+++ b/dist/types-CtkD4o1_.d.ts
@@ -72,6 +72,16 @@ interface WarpGrepClientConfig {
     timeout?: number;
     /** Retry configuration */
     retryConfig?: RetryConfig;
+    /** WarpGrep model id (defaults to morph-warp-grep-v2.1) */
+    model?: string;
+    /** Sampling temperature for the WarpGrep model (defaults to 0) */
+    temperature?: number;
+    /** Maximum tokens per WarpGrep model turn (defaults to 2048) */
+    maxTokens?: number;
+    /** Maximum agent turns before forced finish (defaults to 6) */
+    maxTurns?: number;
+    /** Omit the tools array from chat/completions requests, relying on Morph's built-in tools. Defaults to false. */
+    useBuiltinTools?: boolean;
 }
 /**
  * Input for warp grep execution
@@ -98,6 +108,16 @@ interface WarpGrepInput {
     streamSteps?: boolean;
     /** Search type hint. Use 'node_modules' to search inside normally-excluded dependency directories. */
     search_type?: 'default' | 'node_modules';
+    /** Per-call override of WarpGrep model id */
+    model?: string;
+    /** Per-call override of sampling temperature */
+    temperature?: number;
+    /** Per-call override of max_tokens */
+    maxTokens?: number;
+    /** Per-call override of maxTurns */
+    maxTurns?: number;
+    /** Per-call override of useBuiltinTools */
+    useBuiltinTools?: boolean;
 }
 /**
  * Input for searching a public GitHub repository
@@ -123,6 +143,24 @@ interface WarpGrepContext {
     /** Line ranges returned (e.g. [[1,50],[100,150]]) or '*' for full file */
     lines?: '*' | Array<[number, number]>;
 }
+/**
+ * Per-turn timing metrics captured by the WarpGrep agent.
+ */
+interface WarpGrepTurnTimings {
+    turn: number;
+    morph_api_ms: number;
+    local_tools_ms: number;
+}
+/**
+ * Aggregated timing metrics for a WarpGrep run.
+ */
+interface WarpGrepTimings {
+    turns: WarpGrepTurnTimings[];
+    timeout_ms?: number;
+    initial_state_ms?: number;
+    finish_resolution_ms?: number;
+    total_ms?: number;
+}
 /**
  * Result from warp grep execution
  */
@@ -135,6 +173,8 @@ interface WarpGrepResult {
     summary?: string;
     /** Error message if search failed */
     error?: string;
+    /** Timing metrics captured during the run. Present when the SDK exposes them. */
+    timings?: WarpGrepTimings;
 }
 type GitHubSearchToolConfig = Pick<WarpGrepClientConfig, 'morphApiKey' | 'morphApiUrl' | 'codeSearchUrl' | 'timeout'>;
 /**
@@ -203,6 +243,16 @@ interface WarpGrepToolConfig {
     description?: string;
     /** Search type hint. Use 'node_modules' to search inside normally-excluded dependency directories. */
     search_type?: 'default' | 'node_modules';
+    /** WarpGrep model id (defaults to morph-warp-grep-v2.1) */
+    model?: string;
+    /** Sampling temperature (defaults to 0) */
+    temperature?: number;
+    /** Maximum tokens per WarpGrep model turn (defaults to 2048) */
+    maxTokens?: number;
+    /** Maximum agent turns before forced finish (defaults to 6) */
+    maxTurns?: number;
+    /** Omit the tools array from chat/completions requests. Defaults to false. */
+    useBuiltinTools?: boolean;
 }
 
-export type { GitHubSearchInput as G, RemoteCommands as R, WarpGrepClientConfig as W, WarpGrepInput as a, WarpGrepResult as b, WarpGrepContext as c, WarpGrepToolConfig as d, GitHubReadFileInput as e, GitHubReadFileResult as f, GitHubSearchToolConfig as g, GitHubReadFileToolConfig as h };
+export type { GitHubSearchInput as G, RemoteCommands as R, WarpGrepClientConfig as W, WarpGrepInput as a, WarpGrepResult as b, WarpGrepContext as c, WarpGrepToolConfig as d, GitHubReadFileInput as e, GitHubReadFileResult as f, GitHubSearchToolConfig as g, GitHubReadFileToolConfig as h, WarpGrepTimings as i, WarpGrepTurnTimings as j };
